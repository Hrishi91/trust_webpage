# Build log (append-only, newest at bottom)

## 2026-09-03 — v0.0.1 scaffold
Repo created; design spec approved; docs discipline + pre-commit hook; dev-only
package.json (firebase-tools, rules-unit-testing, playwright). No site code yet.
Fix: test scripts use globs (Node 24), rules-unit-testing ^5 (firebase 12 peer). package-lock synced to 5.0.2.

v0.0.2 — firebase.json (emulator ports 8080/9099/9199/4000), .firebaserc, firebase-config.js placeholder (PASTE), deny-all rules stubs, setup-firebase.md guide (Bengali).

v0.1.0 — i18n module (getLang/setLang/pick/t/STRINGS/onLangChange), 11 unit tests.

v0.2.0 — ui helpers (escapeHtml, countdown, bnDigits, fmtDate, el, toast), 9 unit tests.
Fix: Bengali digit codepoints (was Devanagari 1–9), date-only fmtDate parses as local, 9 tests.

v0.3.0 — Firestore rules (default deny; published+deleted gates; admins console-only; audit append-only) + emulator test matrix (9 tests). Mutation checks: dropping the deleted clause fails history/events/albums; dropping audit uid check fails audit.
Fix: settings/site delete denied; committee/photo deleted guards, hasDeletedFlag and legacy-doc reads now under test; mutation 3 (hasDeletedFlag) verified.

v0.4.0 — Storage rules (public/ read-all, admin write via firestore.exists, under 5 MB, image/pdf only) + 4 emulator tests; scripts/deploy-rules.sh (tests gate deploy). Mutations: dropping okType fails .exe test; dropping firestore.exists fails other-user upload.
Fix: update-op, anon-delete, admin-private-read, list, exact-5MB, anchored-type tests; clearStorage in _env; size mutation verified; deploy script pins --project.

v0.5.0 — js/firebase.js single entry (SDK 12.18.0 CDN, persistent local cache, App Check when key set, emulator wiring on localhost); smoke-tested against emulator with Playwright.
Fix: projectId overridden to demo-trust on localhost — singleProjectMode does not merge namespaces; admin gate now sees seeded data.

v0.6.0 — admin shell: email login, admins-doc gate, hash router, dashboard grid, reauth helper, bn/en toggle; verified against emulator with Playwright (wrong pw, admin, non-admin rejected, persistence, logout).
Fix: route() awaits render inside try/catch (sync throws now caught); login/reauth errors localized.
Fix: sections/registerSection moved from admin.js into new admin/js/registry.js. A section file does `import { registerSection } from '../admin.js'` and calls it at its own top level; admin.js statically imports every section file at its bottom — a circular module reference. With the Map declared directly in admin.js, that call ran (per ES module evaluation order) before admin.js's own `const sections = new Map()` line, hitting it in its temporal dead zone and crashing the whole app the moment any section was uncommented (reproduced with a throwaway `admin/js/sections/_boom.js`, matching task-9-brief.md's and task-10-brief.md's literal registration pattern). A dynamic `await import(...)` does not fix this either — it makes admin.js an async module, and a static back-edge into a still-evaluating async module deadlocks instead of erroring (confirmed empirically: the import promise never resolves, no error). registry.js has no imports of its own, so it is always fully evaluated before anything that touches it runs, regardless of which side of the cycle loads first; admin.js now imports sections/registerSection from there and re-exports registerSection so section files are unaffected.

v0.7.0 — admin forms toolkit (biField/textField/boolField/listView/saveDoc/softDelete), audit log helper, Settings section (trust info, contacts, reg/80G/UPI, puja date, theme, section visibility, maintenance); verified against emulator with Playwright (save → reauth → audit row).
Fix: pujaDate round-trips into datetime-local (toLocalInput helper); reorder swap is one writeBatch.

v0.8.0 — admin Export: all collections + album photos as one JSON download (audit row logged); verified with Playwright download against emulator.
Includes settings, history, committee, albums (with nested photos), events, and audit collections. Timestamps serialize as {seconds, nanoseconds}. Object URL revoked after download to prevent memory leak.
Fix: export errors surface as a toast; button disabled while running.

v0.9.0 — Pages placeholder index + .nojekyll; deploy guide. GitHub repo creation ⏳ owner (token lacks repo-create permission); Pages/rules deploy/domain/App Check follow.

v0.10.0 — js/content.js: DEFAULT_SETTINGS, getSettings (memoised, merged defaults), listPublished/listCommittee/listPhotos/getPublished — published-only queries; smoke-tested in browser against emulator.

v0.11.0 — public shell: css/site.css theme, js/shell.js (header/nav/footer from settings, lang toggle, maintenance mode, sectionVisibility nav filter), index.html skeleton + home stub; verified with Playwright (bn/en, maintenance, 375px no overflow).

v0.12.0 — home page: hero (name/tagline/theme), puja countdown (bn digits, 'today' state, 60s tick), next 3 upcoming events, latest album card; sections omitted when empty; verified with Playwright.
Fix: `main.replaceChildren(...)` is the native DOM method, not `el()` — it stringifies non-Node arguments instead of skipping them, so a conditional section written as `cond ? el(...) : null` (or the brief's original `cond && el(...)`) left a literal "null" (or "0") text node in #main when the condition was false. Sections are now built into an array and passed as `main.replaceChildren(...arr.filter(Boolean))`.

v0.13.0 — History admin section (year/title/body/images, draft/publish, preview, soft delete) + About page rendering sanitized rich text (DOMPurify 3.2.4 SRI-pinned); XSS probe (img onerror, script, javascript: href) verified stripped.
Fix: `el()`'s child loop only skips `null`/`undefined` (not `false`), so the brief's `id !== 'new' && el(...)` inside `history.js`'s toolbar would have rendered a literal "false" text node when creating a new entry — used `id !== 'new' ? el(...) : null` instead, consistent with the v0.12.0 `replaceChildren` fix already applied to `about.js`.

v0.14.0 — image upload: resize.js fitDims (5 tests) + upload.js (client-side resize to WebP, resumable upload to public/, imageField/multiImageField widgets with progress); el() now skips false children; verified against Storage emulator with Playwright (admin ok, non-admin denied).
Fix: EXIF orientation honoured (from-image); imageField input reset for retry; capture attr intentionally omitted.

v0.15.0 — Committee: admin section (name/post/photo upload/show-on-website, reorder, soft delete) + public committee page (grid of persons, placeholder avatar, bn/en); verified with Playwright.

v0.16.0 — Gallery: albums admin (title/year/cover, draft/publish/preview, multi-photo upload with progress, captions, photo reorder via batch, soft-delete photo) + public gallery (album grid newest-first, album page, lightbox); verified with Playwright incl. draft isolation.
Fix: cover-photo auto-set from the first uploaded photo was fragile — `f.cover.read()` never reflects that auto-set (only the cover widget's own upload), so every photo (not just the first) overwrote `coverUrl`, and a later Save/Publish click in the same session stomped it back to `''`. A local `coverAutoSet` flag now gates the auto-set to once, and `read()` omits `coverUrl` from the save payload unless the widget was actually used, letting `setDoc({merge:true})` leave the auto-set value alone.
Fix: multi-upload widget persists across photo re-renders (no mid-upload teardown); cover auto-set reflected in form via imageField.set().

v0.16.1 — admin gate: opening a second same-origin tab initialised Auth with the SDK's default indexedDBLocalPersistence while the admin tab used explicit browserLocalPersistence, and the SDK's cross-tab persistence sync then cleared the admin tab's session outright (`onAuthStateChanged(null)`, no error, no `isAdmin()` call, no network request) — `js/firebase.js` now pins every tab to `browserLocalPersistence` unconditionally, removing the mismatch — transient Firestore errors no longer sign the admin out; only permission-denied does; gate not re-run for the same uid.
Fix: Auth persistence fixed at construction via initializeAuth (no post-hoc setPersistence window).

v0.17.0 — Events: admin section (title/start/end/venue/desc, draft/publish, chronological list, soft delete, empty-start guard) + public events page (upcoming/past split, bn/en); verified with Playwright incl. datetime round-trip.

v0.18.0 — emulator seed (firebase-admin, refuses non-emulator hosts) + Playwright e2e (8 specs: public pages, lang persistence, draft isolation, mobile overflow, anon preview error, admin wrong-pw, create+publish event, soft delete with reauth); home/about pages now surface fetch errors.

v0.19.0 — docs: full admin guide, go-live checklist, pending/context/README refreshed to the real state; live steps ⏳ owner.
Fix: soft-delete e2e asserts history/h1 still exists with deleted:true (mutation-checked against a hard delete); seed guard is a strict host match.

v0.20.0 — fix: declare all nine Firestore composite indexes (firestore.indexes.json, wired into firebase.json/deploy-rules.sh); go-live checklist Step 1 rewritten to the real nine-index list plus the deploy command, and gained the missing owner step (custom domain → Auth Authorized domains + GCP API-key HTTP referrers); js/content.js's three bare catches (getSettings, listPhotos, getPublished) now console.warn the swallowed error before falling back.

v0.21.0 — fix: firestore.rules/storage.rules isAdmin() now also requires request.auth.token.email_verified == true, so an admin account whose email was never verified in the console cannot write anywhere. tests/rules/_env.js gained an `unverified` context (same ADMIN_UID, email_verified: false) and firestore.test.js/storage.test.js assert it cannot write settings/site or upload to public/. Mutation-checked: reverting isAdmin() to drop the email_verified clause failed exactly those two tests (13/15 pass, the two new assertions fail) and nothing else; reverted back to 15/15 green. setup-firebase.md now tells the owner to tick Email verified on the admin user in the console.

v0.22.0 — fix: photo delete in the gallery admin (admin/js/sections/albums.js) now requires reauth (ctx.reauth()) after confirm(), matching every other delete in the app (history/events/albums/committee all go through forms.js's softDelete, which already reauths) — the photo row's own inline delete button was the one soft-delete path that skipped it. Also sets updatedAt on the soft-delete write, consistent with softDelete().

v0.23.0 — fix: per-page browser titles (js/shell.js's mountShell(active, pageTitle) renders "<page> · <trust name>"; about/committee/gallery/events pass t('nav.*'), home passes nothing) and page-specific <meta description> on all five public HTML files (previously identical boilerplate on every page). js/content.js's getSettings() now fails CLOSED on a read error — { ...DEFAULT_SETTINGS, maintenance: true } instead of the plain defaults — so a broken settings read shows the maintenance notice rather than a silently "open" site; the failure is not memoised (settingsPromise reset in the catch), so the next call retries. Documented in a code comment and in PROJECT_CONTEXT.md's decisions table (content.js has no Firebase-free path to unit-test this, per the brief).

v0.24.0 — fix wave polish: js/rich.js now console.warn('[rich] DOMPurify not loaded — rich text hidden') and renders a bilingual "text could not be shown" <p class="muted"> fragment (new i18n key common.richUnavailable) instead of silently returning an empty fragment when window.DOMPurify is missing. admin/js/sections/history.js and albums.js now guard an empty/NaN year on Save-draft the same way events.js already guards an empty start (toast + return, no save) — Save-draft is type=button and bypasses the input's `required`. scripts/pre-commit-docs.sh's code regex now also covers package.json/firebase.json/.firebaserc/firestore.indexes.json. js/firebase.js console.warns when not local and the App Check site key is still the PASTE placeholder. admin/js/admin.js's isAdmin() "couldn't verify" path now also shows the login form (previously left both #adm-login and #adm-main hidden — a blank page) alongside the existing error toast. docs/user-guide/deploy.md's verification step no longer implies curl can see the JS-rendered <h1>; it checks the static <title> instead and says to use a browser/headless check for the heading. tests/seed/seed.js's albums now use the year*1000 order convention (2025000/2024000) matching admin/js/sections/albums.js's read(). tests/rules/firestore.test.js gained admin LIST assertions on audit and committee (Export depends on both). docs/pending.md's deferred-minors list updated to drop the items resolved across this fix wave.

## 2026-09-03 — v0.19.1 repo live
GitHub repo `Hrishi91/trust_webpage` (underscore — owner created it manually; token cannot create repos) — main pushed (47 commits). Pages enable via API 403 (token lacks Administration on this repo) → owner enables in Settings → Pages. deploy.md paths updated to `/trust_webpage/`.

## 2026-09-03 — v0.19.2 real firebase config
Real web config for `ganesh-puja-trust` pasted into js/firebase-config.js (public by design). App Check key still PASTE.

## 2026-09-03 — v0.20.0 firestore rules + indexes LIVE
Owner: Firestore (default, Native), Auth email/password, admin user + admins/{uid} doc, web app config, `firebase login`. Deployed after `npm test` (unit 25, rules 15): firestore.rules + 9 composite indexes to ganesh-puja-trust. Live REST verification: anon read settings/site → 404 (allowed, no doc yet), anon read admins/{uid} → 403, anon write settings/site → 403. Pending owner: Pages enable (404), Email verified on admin user, App Check key, Blaze + Storage bucket (storage.rules not deployable until then).

## 2026-09-03 — v0.20.1 site LIVE on GitHub Pages
Owner enabled Pages → https://hrishi91.github.io/trust_webpage/ 200 on all 5 pages + /admin/. Indexes finished building ~6 min after deploy (until then home showed the loud error state — as designed). Home renders hero from real Firestore defaults; admin login form renders. Next: owner login test, App Check key, Blaze+Storage.

## 2026-09-03 — v0.20.2 admin save errors surfaced
Live incident: admin saved Settings on production; Firestore denied the write (stale ID token missing email_verified) and the admin saw nothing — the form's async onsubmit rejected silently, no toast, no console noise visible to them. Fix: forms.js's saveDoc/softDelete and settings.js's form.onsubmit now wrap their Firestore calls in try/catch — console.error + toast(common.error) on any failure, with a specific toast (new key admin.emailUnverified) when the denial is `permission-denied` and `ctx.user.emailVerified` is false. saveDoc/softDelete re-throw after toasting so callers that navigate on success don't navigate on failure; the four save/delete call sites in history.js, committee.js, albums.js, events.js now wrap those calls in try/catch (toast already shown inside forms.js) to stop the re-throw from surfacing as an unhandled rejection. Verified against the emulator with Playwright: happy-path settings save toasts সেভ হয়েছে; revoking admins/{uid} mid-session then saving settings (and creating a history doc) toasts .toast-err with zero pageerror events and no navigation to a new id. npm run test:unit (25) and npm run seed && npm run e2e (8) green.

## 2026-09-03 — v0.20.3 dummy content on production
Admin email verified (throwaway local page → sendEmailVerification; auth:export confirms). Owner's first Settings save had silently failed (stale token, no toast) → v0.20.2 fix. Dummy content written via Firestore REST with the firebase-tools owner OAuth token (bypasses rules — dummy only; real content goes through /admin/): settings/site, history ×2, committee ×3, events ×3 (1 past), albums/a2025 + 3 photos (placehold.co images — no Storage bucket yet). Live verified in browser: home (hero, theme, bn countdown, 2 upcoming, album card), gallery album page renders 3 photos; photos composite index confirmed via REST. Still ⏳ owner: App Check key, Blaze + Storage, domain.

## 2026-09-04 — v0.21.0 storage LIVE
Owner upgraded to Blaze. Via firebasestorage API (owner OAuth): enabled API, created default bucket ganesh-puja-trust.firebasestorage.app in asia-south1 (matches config). Deployed storage.rules. Live REST: anon read public/* → 404 (allowed), anon read private/* → 403, anon upload → 403. Budget alert ₹100 (50/90/100%) attempted via Billing Budgets API — see next entry for result.

Budget via Billing Budgets API → 404 (billing-account scope); owner sets it in GCP console instead.

## 2026-09-04 — v0.21.1 storage read path verified with a real object
Dummy 600px WebP (canvas via Playwright) uploaded to public/committee/dummy-c2.webp with the owner OAuth token (GCS JSON API), committee/c2.photoUrl set to the Firebase download URL. Anon GET → 200 image/webp 5824B; committee.html renders it (naturalWidth 600). Upload-through-admin-panel still to be tested by the owner on a phone.

## 2026-09-04 — v0.21.2 full demo dataset on production
Owner asked for a full presentation. Generated canvas WebP placeholders (Playwright) → Storage public/{site,history,committee,albums}; Firestore: settings (logo, address, contacts), history 2021–2025 (rich HTML, 7 images), committee ×8 (Storage portraits), albums 2023/2024/2025 × 6 photos, events ×8 (5 upcoming, 3 past). 43 docs via owner OAuth REST (dummy only). Live verified per page in the browser pane (counts + Storage image hosts). All of it is replaceable from /admin/.

## 2026-09-04 — v1.1.0 phase 2–4 rules
TDD (RED→GREEN): firestore.rules gains explicit rules for the six new Phase 2–4 collections — `donations` (public read only `showOnWall==true && deleted==false`, admin all, `phone` field forbidden on write, no delete), `transparency` and `announcements` (public read via `isLive()`, admin all, no delete), `members` (read gated to the owning phone via `isMember()`/`myPhone()`, or admin; no delete), `notices`/`roster` (read gated to `activeMember()` — signed-in phone with an active `members` doc — plus `isLive()`, or admin; no delete) — plus helpers `isMember()`, `myPhone()`, `activeMember()` (the last does a billed `get()` per query, accepted per spec §6.6). tests/rules/_env.js seeds three `members` docs (`+919999999999` active, `+918888888888` active, `+917777777777` inactive) inside the rules-disabled admin seed and returns three new contexts (`member`, `otherMember`, `inactive`) keyed by `phone_number` claim. tests/rules/firestore.test.js gained 5 tests (donations, transparency, announcements, members, notices+roster) — suite now 20/20 green (`npm run test:rules`; storage's 6 unchanged). 3 mandatory mutation checks, each applied/verified/reverted: (1) dropped `showOnWall==true` from donations read → only the donations test failed; (2) dropped the `active==true` clause from `activeMember()` → only the notices+roster test failed (inactive-member assertions); (3) dropped `myPhone()==phone` from members read → only the members test failed (cross-member read succeeded). firestore.indexes.json gained 12 composite indexes (donations ×2, transparency ×2, announcements ×2, notices ×2, roster ×3 incl. `memberPhones` ARRAY_CONTAINS, members ×1) — 9→21 total. No deploy; local files only.

Fix (security review, round 1): removed (soft-deleted) members lose access — `activeMember()` was checking only `active==true`, so an admin soft-deleting a member (`deleted:true`) while `active` stayed `true` left them able to keep reading `notices`/`roster`; now a shared `me()` helper (single cached `get()`) backs `active==true && deleted==false`, and the `members` own-doc read rule itself now also requires `resource.data.deleted == false` (a soft-deleted member can no longer even read their own doc). isLive() tested on notices/roster — new `notices/draft`, `notices/gone`, `roster/draft`, `roster/gone` seeds assert direct-get fails for both an active member and that a `deleted`-only list query (missing `published`) is rejected. donations closed key set — create/update now requires `request.resource.data.keys().hasOnly([...14 allowed fields...])`, closing the nested-object loophole (`{contact:{phone:...}}` bypassed the old flat `!('phone' in data)` check) and any other unlisted key. hasDeletedFlag tested on all six new collections (missing-`deleted` set and non-bool `deleted` update both assert-fail) — previously only asserted on the original four. +4 indexes: `notices`/`roster`/`announcements` (deleted ASC, order ASC — admin list-view shape) and `donations` (deleted ASC, date DESC) — 21→25 total. `isMember()` gained an E.164 shape guard (`^\+[0-9]{10,14}$`) so a malformed `phone_number` claim can't satisfy membership checks. Renamed the stale "unknown collections" test (its target collections gained real rules this same task) to `zzz_unknown/x`, a path genuinely unmatched by any rule. tests/rules/_env.js gained a 4th member `+916666666666` (`active:true, deleted:true`) and context `removed`. Suite still 20/20 green (`npm run test:rules`; 14 firestore + 6 storage). 3 more mutation checks, each applied/verified/reverted: (4) dropped `isLive()` from notices/roster read → only the notices+roster test failed; (5) dropped `deleted==false` from `me()` in `activeMember()` → only the notices+roster test failed (`removed` context wrongly passed); (6) dropped `hasOnly(...)` from donations write → only the donations test failed (nested-phone doc wrongly succeeded). No deploy; local files only.

## 2026-09-04 — v1.1.1 phase 2–4 shared plumbing
TDD (RED→GREEN): new `js/money.js` (pure, no DOM/Firebase) — `sum(items)`, `inr(n, lang)` (Indian grouping via `Intl.NumberFormat('en-IN', { maximumFractionDigits: 0 })`, Bengali digits via `bnDigits` from `js/ui.js` when `lang==='bn'`; Node 24 already produces `12,34,567` grouping, no manual implementation needed), `balance(pledge, payments)`. `tests/unit/money.test.js` (6 tests) — `npm run test:unit` 25→31 green.
`js/i18n.js` gained `nav.donate/transparency/members`, `donate.*` (9 keys), `tr.*` (7 keys), `live.*` (3 keys), `mem.*` (12 keys), `admin.*` for the six new sections (8 keys).
`js/content.js` gained seven reads following the existing `rows()`/try-catch/`console.warn('[content]', …)` pattern: `listDonorWall(limitN=50)`, `listTransparencyYears()`, `getTransparency(year)`, `onAnnouncements(cb)` (realtime `onSnapshot`, published+!deleted, non-expired client-side filter, pinned-then-order sort, `cb(list, {live})`), `getMyMember(phone)`, `listNotices()`, `listMyRoster(phone)` — all query shapes per Task 1's rules (`showOnWall`/`published`+`deleted` gates, `memberPhones array-contains`).
`js/shell.js` NAV gained `donate`/`transparency`/`members` entries (visibility-gated, same as existing sections). `js/firebase.js` re-exports `arrayUnion`, `RecaptchaVerifier`, `signInWithPhoneNumber` (imported from the pinned 12.18.0 CDN modules; `limit`/`onSnapshot` already exported).
`admin/js/upload.js` gained `fileField(ctx, label, currentUrl, { folder, accept='application/pdf', maxBytes=5MB })` mirroring `imageField` minus resize — uploads the raw file via `uploadPublic` with its own `type`, renders a link preview instead of a thumbnail; an oversized file toasts `common.error`, resets `input.value`, and leaves the previous URL untouched (checked before any async work, so the reset is synchronous).
Verified against the Firestore/Auth/Storage emulators (seeded, project `demo-trust`): toggled `settings/site.sectionVisibility.{donate,transparency,members}` via REST (`Authorization: Bearer owner`) — home nav rendered 8 links with the flags `true`, 5 with them `false`. `import('./js/content.js')` in the page confirmed all seven new exports are functions and `onAnnouncements` invoked its callback with `[]`/`{live:false}` on the empty seed; `listDonorWall`/`listTransparencyYears`/`getTransparency`/`listNotices`/`listMyRoster`/`getMyMember` all resolved without error. `fileField` mounted in a throwaway (uncommitted) harness page signed in as the seeded admin: a 165-byte fake PDF uploaded and rendered a link to a Storage emulator URL; a 6 MB blob toasted `common.error` (`.toast-err`), reset the input, and `read()` still returned the prior upload's URL. Emulators, static server, and browser tabs all stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free.

## 2026-09-04 — v1.2.0 donations admin
New `admin/js/sections/donations.js` (pattern: history.js/events.js), registered in `admin/js/admin.js` (dashboard now 7 tiles — `tests/e2e/admin.spec.js` tile count updated 6→7). `#donations`: single query `where('deleted','==',false), orderBy('date','desc')`, grouped client-side by `year` — year `<select>` (years present ∪ current year, default current year or latest present) + summary card (count, `inr(sum())` total, per-mode cash/upi/bank totals, wall count) + list rows `date · name (or donate.anonymous when isAnonymous) · ₹amount · mode` with a wall badge. `#donations/new|<id>`: form with donorName/amount/date/mode(select)/receiptNo(auto-suggested `R-${year}-${Date.now().toString(36).toUpperCase()}` for new only)/isAnonymous/showOnWall(default true)/note; guards empty name, amount ≤ 0, invalid date → toast + return; single Save button (no draft state for donations — always `saveDoc(ctx,'donations', id, data, { publish: true })` with only the closed-set keys); delete via `softDelete`.
Verified against the Firestore/Auth emulators (`demo-trust`) with a throwaway Playwright script (`npm run emu` + `npm run serve`, admin seeded via `npm run seed`): created 3 donations (one `isAnonymous`, one `showOnWall:false`, two different years) — this-year summary count 2 / ₹1,700 total / cash ₹500 / upi ₹1,200 / bank ₹0 / wall 2 correct; last-year summary count 1 / ₹300 / bank ₹300 / wall 0 correct; edited one donation's amount 500→750, this-year total recalculated to ₹1,950 correctly; soft-deleted the last-year donation — it dropped out of the list and its year vanished from the `<select>` entirely (no more active donations that year). REST read-back (`Authorization: Bearer owner`) on all 3 docs: exactly the closed-set keys (`date,mode,donorName,note,amount,isAnonymous,deleted,year,receiptNo,published,showOnWall,order,createdAt,updatedAt`), no `phone` key anywhere. Zero console/page errors throughout. Emulators, static server, and the browser all stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free.

## 2026-09-04 — v1.2.1 donate page
New `donate.html` + `js/pages/donate.js` (pattern: committee.js/events.js — `mountShell('donate', t('donate.title'))`, `sectionVisibility.donate === false` → `common.empty` only, `listDonorWall()` fetched once with `langchange` re-render). Content: UPI card (`s.upiId` set) — big `<code class="upi-id">` id + copy button (`navigator.clipboard.writeText`, guarded when unavailable, `toast(t('donate.copied'))` on success — new i18n key), QR `<img>` when `s.upiQrUrl` is set, and an `<a class="btn" href="upi://pay?pa=<upiId>&pn=<encodeURIComponent(pick(s.name))>&cu=INR">`; else a `.notice` with `donate.soon` + a WhatsApp link when `s.contacts.whatsapp` is set. Confirm card (only when `s.contacts.whatsapp` is set): name/amount/UPI-ref inputs → guards `amount > 0` (toast `common.error`, same pattern as `admin/js/sections/donations.js`'s save guard) → builds `donate.confirmMsg` with `{amount}/{ref}/{name}` replaced (missing ref/name become `—`) → `window.open('https://wa.me/<whatsapp>?text=...', '_blank', 'noopener')`; nothing is persisted. `s.has80G` → `donate.tax80g` line; `s.regNo` → `tr.regNo` line. Donor wall rows (`.donor`): anonymous label or `donorName` (plain string, not bilingual — matches the admin donations list) · `inr(amount, lang)` · `fmtDate(date, lang)`; empty → `common.empty`. `css/site.css` gained `.btn` (not previously present there, only in `css/admin.css`), `.donor` (flex row, bottom border), `.upi-id` (larger, word-break) — per the brief's "check first" instruction.
Verified against the Firestore/Auth emulators (`demo-trust`, base seed via `npm run seed` + a throwaway REST script with `Authorization: Bearer owner`): `settings/site` patched with `upiId 'trust@upi'`, `upiQrUrl` a placehold.co QR, `has80G true`, `regNo 'WB/123'`, `contacts.whatsapp '919800000000'`, `sectionVisibility.donate true`; 4 `donations` docs (2 wall rows — one `isAnonymous` — 1 `showOnWall:false`, 1 `deleted:true`). Browser-driven checks: h1 present; UPI id `trust@upi` shown; QR `<img>` rendered; pay link `href` contains `upi://pay?pa=trust%40upi&pn=...&cu=INR`; copy button (clipboard stubbed via `Object.defineProperty`) wrote `trust@upi` and showed `.toast.toast-ok` "কপি হয়েছে"; confirm form (name `X`, amount `500`, ref `ABC`) with `window.open` intercepted → URL `https://wa.me/919800000000?text=...` decoding to `আমি ₹500 দান করেছি (UPI ref: ABC)। নাম: X`; amount `0` → blocked, `.toast.toast-err` `common.error`, no `window.open` call; donor wall showed exactly 2 `.donor` rows (anonymous one first, newest-date-first), hidden/deleted rows absent; `has80G` line and `Reg. no. WB/123` shown; language toggle → all-English strings (`Donate`, `Pay via UPI`, `Copy`, `Scan the QR`, `Confirm on WhatsApp`, `Tax-deductible under 80G`, `Donor wall`, `Anonymous`); `upiId ''` → `donate.soon` notice + WhatsApp link, no UPI card, confirmed via REST toggle back afterward; 375px mobile viewport `scrollWidth - clientWidth === 0`. Zero console errors from the page itself (the pre-existing, page-independent `identitytoolkit accounts:lookup` 400 on every page load was confirmed present on `committee.html` too, unrelated to this change). `npm run test:unit` 31/31 green (unaffected by this task; no rules/logic-module changes). Emulators, static server, and the browser all stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free.

## 2026-09-04 — v1.2.2 transparency admin
New `admin/js/sections/transparency.js` (pattern: donations.js/history.js), registered in `admin/js/admin.js` after the donations import (dashboard now 8 tiles — `tests/e2e/admin.spec.js` tile count updated 7→8). Doc shape exactly per brief: `{year, income:[{category:{bn,en}, amount}], expense:[...], documents:[{title:{bn,en}, url}], notes:{bn,en}, order:year, published, deleted}`, doc id = year string. `#transparency`: single query `where('deleted','==',false), orderBy('year','desc')` — rows `year · income ₹total · expense ₹total` + draft/published badge, `+ New`. `#transparency/new|<year>`: year field (number, required, disabled on edit — it's the doc id); two dynamic tables built with a local `rowsSection()` helper (biField category + textField amount + ✕ remove + `t('admin.addRow')`), each with a live `inr(sum())` total line recomputed via a single delegated `input` listener on the row list (fires on typing and on add/remove); a live balance line (`tr.balance`) above the documents section recomputes off both tables; documents section (`documentsSection()` helper) — biField title + `fileField(ctx, …, {folder:`public/transparency/${year}`})` + remove; `biField` notes (multiline); Save draft / Publish / Preview (`../transparency.html?year=<y>&preview=1`, edit-only) / Delete (edit-only, via `softDelete`). Guards in a shared `validate()`: year must match `/^\d{4}$/`; every income/expense row's amount must be a finite number ≥0 (toast `common.error` and abort otherwise); empty-category rows are dropped by each table's own `read()`; a documents row with no uploaded URL is dropped by `documentsSection()`'s `read()`. Create-time duplicate guard: `save()` does an extra `getDoc(doc(ctx.db,'transparency',String(year)))` before `saveDoc` when `isNew`, and toasts `common.error` + returns (no write, no navigate) if that year's doc already exists — never silently overwrites.
Fixed a real bug caught during verification, before commit: `rowsSection()`'s own construction called a shared `recompute()` that also invoked the caller's `onAnyChange` (used to refresh the cross-table balance line) — since `const income = rowsSection(...)` and `const expense = rowsSection(...)` are still being assigned while `rowsSection` is constructing the *other* one, `onAnyChange`'s closure over `income`/`expense` hit them in their temporal dead zone and crashed the whole form (`ReferenceError: Cannot access 'income' before initialization`, caught by admin.js's route() try/catch and shown as the generic error state). Fixed by splitting `recompute()` into `updateTotal()` (this table's own total line only, safe to call during construction) and `recompute()` (`updateTotal()` + `onAnyChange()`, only ever invoked later from row-add/remove/`input` events, never during initial construction).
Verified against the Firestore/Auth/Storage emulators (`demo-trust`, seeded via `npm run seed`) with a throwaway Playwright script (`npm run emu` + `npm run serve`): logged in, switched to English, created year 2025 with 3 income rows (চাঁদা 42000, রোড 8000, বাস 5000) and 2 expense rows (প্রতিমা 20000, মণ্ডপ 15000) — live totals showed `Total: ₹55,000` / `Total: ₹35,000` correctly; uploaded a generated 128-byte valid PDF (`%PDF-1.4 … %%EOF`) as document "Audit 2025" — file-link appeared after upload; Save draft → toast + list showed the `Draft` badge; Publish (on the resulting edit view) → toast + list showed `Published`. REST read-back (`Authorization: Bearer owner`): shape matches exactly (year, 3 income entries summing 55000, 2 expense entries summing 35000, 1 document with `title.en` "Audit 2025", `published:true`, `deleted:false`); `documents[0].url` fetched anonymously (no auth header) returned 200 `application/pdf`. Duplicate-year guard: creating a second "new" doc with year 2025 toasted `.toast-err`, stayed on `#transparency/new` (no navigation), and the existing doc's `updateTime` was unchanged before/after — confirmed no overwrite. Oversized-file guard: a 6 MB blob on a documents row toasted `.toast-err` and left no file-link (upload never attempted) — this is `upload.js`'s existing client-side `maxBytes` check, unchanged by this task. Zero console/page errors throughout the whole run. `npm test` (31 unit + 20 rules) green; `npm run e2e` (8 tests, including the updated 8-tile dashboard assertion) green. Emulators, static server, and the browser all stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free; no leftover temp/debug files.

Fix: WhatsApp numbers sanitised to digits (donate + footer); .row on site css; single-pass placeholder replace. `js/ui.js` gained a pure helper `digits(s) => String(s ?? '').replace(/\D/g, '')` (2 new unit tests: `digits('+91 98000-00000') === '919800000000'`, `digits(null) === ''`), used in `js/pages/donate.js` (the `donate.soon` notice's WhatsApp link and the confirm form's `window.open` URL) and `js/shell.js` (footer WhatsApp link) — the WhatsApp link/deep-link now renders only when `digits(s.contacts.whatsapp)` is non-empty, and `wa.me/<digits>` is built from the sanitised value so a settings value like `+91 98000-00000` (spaces/dashes/plus) no longer breaks the `wa.me` URL. `css/site.css` gained `.row { display:flex; gap:.5rem; align-items:center; flex-wrap:wrap; }` (was referenced by `donate.js`'s confirm-form field wrappers but not defined anywhere in this stylesheet — only used unstyled before). `donate.js`'s confirm-message placeholder substitution changed from three chained `.replace('{x}', ...)` calls to a single-pass `msg.replace(/\{(amount|ref|name)\}/g, (_, k) => vals[k])`.
Verified: `npm run test:unit` 33/33 green (31→33). Browser-driven check against the Firestore emulator (`demo-trust`) with `settings/site.contacts.whatsapp` set to `'+91 98000-00000'`: confirm form (name X, amount 500, ref ABC) with `window.open` intercepted → URL `https://wa.me/919800000000?text=...`; footer WhatsApp link `href="https://wa.me/919800000000"`; `.row` computed `display: flex`; zero console errors. Emulators and static server stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free.

## 2026-09-04 — v1.2.3 transparency page
New `transparency.html` + `js/pages/transparency.js` (pattern: donate.js — `mountShell('transparency', t('tr.title'))`, `sectionVisibility.transparency === false` → `common.empty` only). Data: `listTransparencyYears()` fetched once (published+!deleted docs, already carrying the full income/expense/documents/notes fields, no second read needed); when `?preview=1` is present the requested `?year=` doc is also read directly via `getDoc(doc(db,'transparency',yearParam))` (own try/catch, `console.warn` on `permission-denied` so an anon preview request doesn't fail the whole page) and folded into the tab list only if that year isn't already published. Year tabs (`.tabs` buttons, newest first, `?year=` selects if present else the newest) navigate via `location.assign` with the `year` param swapped in (a full reload — simplest way to keep `?preview=1` and other params intact). Selected year's data resolves to the previewed doc when its year matches, else the published list entry, else `null` → `common.empty`; income/expense render as `.table-wrap > table.ledger` (category · `inr(amount)`, plus a `মোট/Total` row via a local `TOTAL_LABEL`), a `.summary` strip (`tr.income`/`tr.expense`/`tr.balance`, `class="neg"` when `income − expense < 0`), documents as `<a target=_blank rel=noopener>` (title only, section omitted when the array is empty), notes via `textContent` (plain, not rendered as rich HTML — matches the brief). Header line combines `tr.regNo <regNo>` and `donate.tax80g` (joined `· `, each half omitted when unset) — new use of an existing key, no new i18n added. `css/site.css` gained `.tabs`/`.table-wrap`/`table.ledger`/`.summary` exactly per the brief's decisions block.
Verified against the Firestore/Auth emulators (`demo-trust`, seeded via `npm run seed`) with a throwaway Admin-SDK script (`tests/seed/_tmp_seed_transparency.mjs`, deleted after the run): `settings/site` patched `sectionVisibility.transparency true`, `regNo 'WB/123'`, `has80G true`; `transparency/2025` published (income 42000+8000+5000 incl. one long category name for overflow testing, expense 20000+15000, 1 document, notes), `transparency/2024` published (income 3000, expense 5000 — expense > income), `transparency/2023` **draft**. Browser-driven checks: default load (no `?year=`) selected 2025, tabs `2025/2024` only (2023 absent); income table showed all 3 rows + `Total ₹55,000`, expense `Total ₹35,000`, summary `Income ₹55,000 / Expense ₹35,000 / Balance ₹20,000` (no `.neg`); document link `href https://example.com/audit-2025.pdf`, `target="_blank"`, `rel="noopener"`; notes text present; header line `Reg. no. WB/123 · Tax-deductible under 80G`. Language toggle → bn: h1 `আয়-ব্যয়ের হিসাব`, tabs `২০২৫`/`২০২৪` (Bengali digits), amounts in Bengali digits, header line in Bengali. Clicking the `2024` tab (in en) navigated to `?year=2024`, selected it (`active` class), showed `Balance: ₹-2,000` with the `.neg` class on that span, documents section omitted (empty array); direct load of `?year=2024` preselected the same. `?year=2023` (draft) as anon: tabs still only `2025/2024`, body `Nothing here yet` (`common.empty`) — no numbers leaked, zero console errors. Logged in at `/admin/` (seeded admin) in the same browser context, then `?year=2023&preview=1`: tab list now included `2023`, draft numbers shown correctly (income ₹1,000, expense ₹500, balance ₹500) with zero console errors on that load (a `console.warn('[transparency] preview', permission-denied)` appears only on the *anon* preview attempt, confirmed via `read_network_requests`/`read_console_messages` scoped to each navigation — never a `console.error`, never surfaced to the anon user as anything but the standard empty state). 375px mobile viewport with the long category name present: `document.body.scrollWidth === document.body.clientWidth` (no page-level horizontal overflow; the table itself scrolls inside `.table-wrap`). `npm test` (33 unit + 20 rules) green; `npm run e2e` (8 tests) green — run correctly this time with `npm run emu` left running + `npm run seed` before `npm run e2e` (the suite doesn't start/seed its own emulators; an earlier same-session attempt without a running emulator produced 4 spurious failures that had nothing to do with this change). Emulators, static server, and the browser all stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free; no leftover temp/debug files.

## 2026-09-04 — v1.3.0 announcements admin
New `admin/js/sections/announcements.js`, registered **first** in `admin/js/admin.js` (`import './sections/announcements.js'; // Phase 3` ahead of `settings.js`, so its tile leads the dashboard — dashboard now 9 tiles, `tests/e2e/admin.spec.js` tile count updated 8→9). Doc shape exactly per brief: `{ text:{bn,en}, pinned, isLive, expiresAt: ISO|'', order: createdAt ms (set once), published, deleted }`. Single route `#announcements` (no sub-routes): a quick-post card (`biField` text multiline, `boolField` pinned, `boolField` isLive, `textField` expiresAt `datetime-local` via `toLocalInput`) with one `t('admin.publish')` button — guards non-empty bn-or-en text (toast `common.error` otherwise), then for a new post `saveDoc(ctx,'announcements', null, {…, order: Date.now()}, {publish:true})`, clears the form, and re-renders the list below; for an in-progress edit (see next) the same card/button instead calls `saveDoc(ctx,'announcements', id, data)` with no `order`/`publish` override, leaving those two fields untouched. List: single query `where('deleted','==','false'), orderBy('order','desc')` (newest first), each row `pick(text) · fmtDate(order, lang)` (display date is the numeric `order`, i.e. creation time, not the Firestore-Timestamp `createdAt` — `fmtDate` accepts a raw ms number natively so this sidesteps ever formatting a Timestamp object) plus badges 📌 (pinned), 🔴 (isLive), `⏳ <date>` when `expiresAt` is set and in the future or the literal "expired"/"মেয়াদ শেষ" when past, and `admin.draft` when unpublished; three `btn-sm` row buttons — Edit (re-opens the quick-post card prefilled, via a shared local `renderForm(cur, id)`), Publish/Unpublish (`updateDoc(ref, {published: !cur, updatedAt: serverTimestamp()})` + `logAudit(ctx, cur?'unpublish':'publish', path, {published:cur}, {published:!cur})`), Delete (`softDelete`). All three row handlers and the form's save handler are wrapped in try/catch with a toast on error (form/delete errors are already toasted inside `saveDoc`/`softDelete`; the publish-toggle handler toasts `common.error` itself since it calls `updateDoc` directly). Field labels (message/pin/live/expires/edit/expired) are local to the section file (`L` object, pattern from `donations.js`) since the brief's toolkit only names `admin.announcements`/`admin.publish`/`admin.unpublish`/`admin.delete`/`admin.saved`/`live.badge`/`common.*` as pre-existing keys — no new `js/i18n.js` keys were needed. No rules or index changes: `announcements` rules (`admin create/update with hasDeletedFlag()`, public `published && !deleted`) and both `(deleted, order DESC)` / `(deleted, order ASC)` indexes already existed from Task 1.
Verified against the Firestore/Auth emulators (`demo-trust`, seeded via `npm run seed`) with a throwaway Playwright script (`tests/seed/_tmp_verify_announcements.mjs`, deleted after the run) plus a throwaway Admin-SDK read-back script (`tests/seed/_tmp_readback_announcements.mjs`, also deleted): logged in, confirmed the dashboard's first tile is `📢 ঘোষণা` (Announcements); posted 3 announcements in sequence — pinned, live, and one with `expiresAt` set to yesterday — the list showed all 3 newest-first (the expired one, posted last, on top) with the correct 📌/🔴/`মেয়াদ শেষ` badges and no `draft` badge (quick-post always publishes). Clicked Unpublish on the top (expired) row → badge set gained `ড্রাফট`; clicked Publish again → badge reverted, confirming the toggle round-trips. Clicked Edit on the pinned row, changed its Bengali text, re-submitted → list showed the updated text with the 📌 badge intact. Deleted the live-message row (confirm + reauth dialogs handled, `password12345`) → it dropped out of the list, leaving 2. REST/Admin-SDK read-back on all 3 docs: exact key set `createdAt,deleted,expiresAt,isLive,order,pinned,published,text,updatedAt` on every doc (matches the brief's required 9 keys exactly); the edited pinned doc's `order` (`1788468913499`) was identical before and after the text edit, confirming edit never touches `order`; the deleted doc showed `deleted:true`. Zero console errors throughout (`page.on('console'|'pageerror')` both empty). `npm run test:unit` 33/33 green (unaffected — no logic-module changes); `npm run test:rules` 20/20 green (unaffected — no rules changes); `npm run e2e` 8/8 green, including the updated 9-tile dashboard assertion. Emulators, static server, and the browser all stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free; no leftover temp/debug files.
