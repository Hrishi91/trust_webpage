# Build log (append-only, newest at bottom)

## 2026-09-03 — v0.0.1 scaffold
Repo created; design spec approved; docs discipline + pre-commit hook; dev-only
package.json (firebase-tools, rules-unit-testing, playwright). No site code yet.
Fix: test scripts use globs (Node 24), rules-unit-testing ^5 (firebase 12 peer). package-lock synced to 5.0.2.

v0.0.2 — firebase.json (emulator ports 8080/9099/9199/4000), .firebaserc, firebase-config.js placeholder (PASTE), deny-all rules stubs, setup-firebase.md guide (Bengali).

v0.1.0 — i18n module (getLang/setLang/pick/t/STRINGS/onLangChange), 11 unit tests.

v0.2.0 — ui helpers (escapeHtml, countdown, bnDigits, fmtDate, el, toast), 9 unit tests.
Fix: Bengali digit codepoints (was Devanagari 1–9), date-only fmtDate parses as local, 9 tests.

v0.3.0 — Firestore rules (default deny; published+deleted gates; admins console-only; audit append-only) + emulator test matrix (9 tests). Mutation checks: dropping the deleted clause fails history/events/albums; dropping audit uid check fails audit.
Fix: settings/site delete denied; committee/photo deleted guards, hasDeletedFlag and legacy-doc reads now under test; mutation 3 (hasDeletedFlag) verified.

v0.4.0 — Storage rules (public/ read-all, admin write via firestore.exists, under 5 MB, image/pdf only) + 4 emulator tests; scripts/deploy-rules.sh (tests gate deploy). Mutations: dropping okType fails .exe test; dropping firestore.exists fails other-user upload.
Fix: update-op, anon-delete, admin-private-read, list, exact-5MB, anchored-type tests; clearStorage in _env; size mutation verified; deploy script pins --project.

v0.5.0 — js/firebase.js single entry (SDK 12.18.0 CDN, persistent local cache, App Check when key set, emulator wiring on localhost); smoke-tested against emulator with Playwright.
Fix: projectId overridden to demo-trust on localhost — singleProjectMode does not merge namespaces; admin gate now sees seeded data.

v0.6.0 — admin shell: email login, admins-doc gate, hash router, dashboard grid, reauth helper, bn/en toggle; verified against emulator with Playwright (wrong pw, admin, non-admin rejected, persistence, logout).
Fix: route() awaits render inside try/catch (sync throws now caught); login/reauth errors localized.
Fix: sections/registerSection moved from admin.js into new admin/js/registry.js. A section file does `import { registerSection } from '../admin.js'` and calls it at its own top level; admin.js statically imports every section file at its bottom — a circular module reference. With the Map declared directly in admin.js, that call ran (per ES module evaluation order) before admin.js's own `const sections = new Map()` line, hitting it in its temporal dead zone and crashing the whole app the moment any section was uncommented (reproduced with a throwaway `admin/js/sections/_boom.js`, matching task-9-brief.md's and task-10-brief.md's literal registration pattern). A dynamic `await import(...)` does not fix this either — it makes admin.js an async module, and a static back-edge into a still-evaluating async module deadlocks instead of erroring (confirmed empirically: the import promise never resolves, no error). registry.js has no imports of its own, so it is always fully evaluated before anything that touches it runs, regardless of which side of the cycle loads first; admin.js now imports sections/registerSection from there and re-exports registerSection so section files are unaffected.

v0.7.0 — admin forms toolkit (biField/textField/boolField/listView/saveDoc/softDelete), audit log helper, Settings section (trust info, contacts, reg/80G/UPI, puja date, theme, section visibility, maintenance); verified against emulator with Playwright (save → reauth → audit row).
Fix: pujaDate round-trips into datetime-local (toLocalInput helper); reorder swap is one writeBatch.

v0.8.0 — admin Export: all collections + album photos as one JSON download (audit row logged); verified with Playwright download against emulator.
Includes settings, history, committee, albums (with nested photos), events, and audit collections. Timestamps serialize as {seconds, nanoseconds}. Object URL revoked after download to prevent memory leak.
Fix: export errors surface as a toast; button disabled while running.

v0.9.0 — Pages placeholder index + .nojekyll; deploy guide. GitHub repo creation ⏳ owner (token lacks repo-create permission); Pages/rules deploy/domain/App Check follow.

v0.10.0 — js/content.js: DEFAULT_SETTINGS, getSettings (memoised, merged defaults), listPublished/listCommittee/listPhotos/getPublished — published-only queries; smoke-tested in browser against emulator.

v0.11.0 — public shell: css/site.css theme, js/shell.js (header/nav/footer from settings, lang toggle, maintenance mode, sectionVisibility nav filter), index.html skeleton + home stub; verified with Playwright (bn/en, maintenance, 375px no overflow).

v0.12.0 — home page: hero (name/tagline/theme), puja countdown (bn digits, 'today' state, 60s tick), next 3 upcoming events, latest album card; sections omitted when empty; verified with Playwright.
Fix: `main.replaceChildren(...)` is the native DOM method, not `el()` — it stringifies non-Node arguments instead of skipping them, so a conditional section written as `cond ? el(...) : null` (or the brief's original `cond && el(...)`) left a literal "null" (or "0") text node in #main when the condition was false. Sections are now built into an array and passed as `main.replaceChildren(...arr.filter(Boolean))`.

v0.13.0 — History admin section (year/title/body/images, draft/publish, preview, soft delete) + About page rendering sanitized rich text (DOMPurify 3.2.4 SRI-pinned); XSS probe (img onerror, script, javascript: href) verified stripped.
Fix: `el()`'s child loop only skips `null`/`undefined` (not `false`), so the brief's `id !== 'new' && el(...)` inside `history.js`'s toolbar would have rendered a literal "false" text node when creating a new entry — used `id !== 'new' ? el(...) : null` instead, consistent with the v0.12.0 `replaceChildren` fix already applied to `about.js`.

v0.14.0 — image upload: resize.js fitDims (5 tests) + upload.js (client-side resize to WebP, resumable upload to public/, imageField/multiImageField widgets with progress); el() now skips false children; verified against Storage emulator with Playwright (admin ok, non-admin denied).
Fix: EXIF orientation honoured (from-image); imageField input reset for retry; capture attr intentionally omitted.

v0.15.0 — Committee: admin section (name/post/photo upload/show-on-website, reorder, soft delete) + public committee page (grid of persons, placeholder avatar, bn/en); verified with Playwright.

v0.16.0 — Gallery: albums admin (title/year/cover, draft/publish/preview, multi-photo upload with progress, captions, photo reorder via batch, soft-delete photo) + public gallery (album grid newest-first, album page, lightbox); verified with Playwright incl. draft isolation.
Fix: cover-photo auto-set from the first uploaded photo was fragile — `f.cover.read()` never reflects that auto-set (only the cover widget's own upload), so every photo (not just the first) overwrote `coverUrl`, and a later Save/Publish click in the same session stomped it back to `''`. A local `coverAutoSet` flag now gates the auto-set to once, and `read()` omits `coverUrl` from the save payload unless the widget was actually used, letting `setDoc({merge:true})` leave the auto-set value alone.
Fix: multi-upload widget persists across photo re-renders (no mid-upload teardown); cover auto-set reflected in form via imageField.set().

v0.16.1 — admin gate: opening a second same-origin tab initialised Auth with the SDK's default indexedDBLocalPersistence while the admin tab used explicit browserLocalPersistence, and the SDK's cross-tab persistence sync then cleared the admin tab's session outright (`onAuthStateChanged(null)`, no error, no `isAdmin()` call, no network request) — `js/firebase.js` now pins every tab to `browserLocalPersistence` unconditionally, removing the mismatch — transient Firestore errors no longer sign the admin out; only permission-denied does; gate not re-run for the same uid.
Fix: Auth persistence fixed at construction via initializeAuth (no post-hoc setPersistence window).

v0.17.0 — Events: admin section (title/start/end/venue/desc, draft/publish, chronological list, soft delete, empty-start guard) + public events page (upcoming/past split, bn/en); verified with Playwright incl. datetime round-trip.

v0.18.0 — emulator seed (firebase-admin, refuses non-emulator hosts) + Playwright e2e (8 specs: public pages, lang persistence, draft isolation, mobile overflow, anon preview error, admin wrong-pw, create+publish event, soft delete with reauth); home/about pages now surface fetch errors.

v0.19.0 — docs: full admin guide, go-live checklist, pending/context/README refreshed to the real state; live steps ⏳ owner.
Fix: soft-delete e2e asserts history/h1 still exists with deleted:true (mutation-checked against a hard delete); seed guard is a strict host match.

v0.20.0 — fix: declare all nine Firestore composite indexes (firestore.indexes.json, wired into firebase.json/deploy-rules.sh); go-live checklist Step 1 rewritten to the real nine-index list plus the deploy command, and gained the missing owner step (custom domain → Auth Authorized domains + GCP API-key HTTP referrers); js/content.js's three bare catches (getSettings, listPhotos, getPublished) now console.warn the swallowed error before falling back.

v0.21.0 — fix: firestore.rules/storage.rules isAdmin() now also requires request.auth.token.email_verified == true, so an admin account whose email was never verified in the console cannot write anywhere. tests/rules/_env.js gained an `unverified` context (same ADMIN_UID, email_verified: false) and firestore.test.js/storage.test.js assert it cannot write settings/site or upload to public/. Mutation-checked: reverting isAdmin() to drop the email_verified clause failed exactly those two tests (13/15 pass, the two new assertions fail) and nothing else; reverted back to 15/15 green. setup-firebase.md now tells the owner to tick Email verified on the admin user in the console.

v0.22.0 — fix: photo delete in the gallery admin (admin/js/sections/albums.js) now requires reauth (ctx.reauth()) after confirm(), matching every other delete in the app (history/events/albums/committee all go through forms.js's softDelete, which already reauths) — the photo row's own inline delete button was the one soft-delete path that skipped it. Also sets updatedAt on the soft-delete write, consistent with softDelete().

v0.23.0 — fix: per-page browser titles (js/shell.js's mountShell(active, pageTitle) renders "<page> · <trust name>"; about/committee/gallery/events pass t('nav.*'), home passes nothing) and page-specific <meta description> on all five public HTML files (previously identical boilerplate on every page). js/content.js's getSettings() now fails CLOSED on a read error — { ...DEFAULT_SETTINGS, maintenance: true } instead of the plain defaults — so a broken settings read shows the maintenance notice rather than a silently "open" site; the failure is not memoised (settingsPromise reset in the catch), so the next call retries. Documented in a code comment and in PROJECT_CONTEXT.md's decisions table (content.js has no Firebase-free path to unit-test this, per the brief).

v0.24.0 — fix wave polish: js/rich.js now console.warn('[rich] DOMPurify not loaded — rich text hidden') and renders a bilingual "text could not be shown" <p class="muted"> fragment (new i18n key common.richUnavailable) instead of silently returning an empty fragment when window.DOMPurify is missing. admin/js/sections/history.js and albums.js now guard an empty/NaN year on Save-draft the same way events.js already guards an empty start (toast + return, no save) — Save-draft is type=button and bypasses the input's `required`. scripts/pre-commit-docs.sh's code regex now also covers package.json/firebase.json/.firebaserc/firestore.indexes.json. js/firebase.js console.warns when not local and the App Check site key is still the PASTE placeholder. admin/js/admin.js's isAdmin() "couldn't verify" path now also shows the login form (previously left both #adm-login and #adm-main hidden — a blank page) alongside the existing error toast. docs/user-guide/deploy.md's verification step no longer implies curl can see the JS-rendered <h1>; it checks the static <title> instead and says to use a browser/headless check for the heading. tests/seed/seed.js's albums now use the year*1000 order convention (2025000/2024000) matching admin/js/sections/albums.js's read(). tests/rules/firestore.test.js gained admin LIST assertions on audit and committee (Export depends on both). docs/pending.md's deferred-minors list updated to drop the items resolved across this fix wave.

## 2026-09-03 — v0.19.1 repo live
GitHub repo `Hrishi91/trust_webpage` (underscore — owner created it manually; token cannot create repos) — main pushed (47 commits). Pages enable via API 403 (token lacks Administration on this repo) → owner enables in Settings → Pages. deploy.md paths updated to `/trust_webpage/`.

## 2026-09-03 — v0.19.2 real firebase config
Real web config for `ganesh-puja-trust` pasted into js/firebase-config.js (public by design). App Check key still PASTE.

## 2026-09-03 — v0.20.0 firestore rules + indexes LIVE
Owner: Firestore (default, Native), Auth email/password, admin user + admins/{uid} doc, web app config, `firebase login`. Deployed after `npm test` (unit 25, rules 15): firestore.rules + 9 composite indexes to ganesh-puja-trust. Live REST verification: anon read settings/site → 404 (allowed, no doc yet), anon read admins/{uid} → 403, anon write settings/site → 403. Pending owner: Pages enable (404), Email verified on admin user, App Check key, Blaze + Storage bucket (storage.rules not deployable until then).

## 2026-09-03 — v0.20.1 site LIVE on GitHub Pages
Owner enabled Pages → https://hrishi91.github.io/trust_webpage/ 200 on all 5 pages + /admin/. Indexes finished building ~6 min after deploy (until then home showed the loud error state — as designed). Home renders hero from real Firestore defaults; admin login form renders. Next: owner login test, App Check key, Blaze+Storage.

## 2026-09-03 — v0.20.2 admin save errors surfaced
Live incident: admin saved Settings on production; Firestore denied the write (stale ID token missing email_verified) and the admin saw nothing — the form's async onsubmit rejected silently, no toast, no console noise visible to them. Fix: forms.js's saveDoc/softDelete and settings.js's form.onsubmit now wrap their Firestore calls in try/catch — console.error + toast(common.error) on any failure, with a specific toast (new key admin.emailUnverified) when the denial is `permission-denied` and `ctx.user.emailVerified` is false. saveDoc/softDelete re-throw after toasting so callers that navigate on success don't navigate on failure; the four save/delete call sites in history.js, committee.js, albums.js, events.js now wrap those calls in try/catch (toast already shown inside forms.js) to stop the re-throw from surfacing as an unhandled rejection. Verified against the emulator with Playwright: happy-path settings save toasts সেভ হয়েছে; revoking admins/{uid} mid-session then saving settings (and creating a history doc) toasts .toast-err with zero pageerror events and no navigation to a new id. npm run test:unit (25) and npm run seed && npm run e2e (8) green.

## 2026-09-03 — v0.20.3 dummy content on production
Admin email verified (throwaway local page → sendEmailVerification; auth:export confirms). Owner's first Settings save had silently failed (stale token, no toast) → v0.20.2 fix. Dummy content written via Firestore REST with the firebase-tools owner OAuth token (bypasses rules — dummy only; real content goes through /admin/): settings/site, history ×2, committee ×3, events ×3 (1 past), albums/a2025 + 3 photos (placehold.co images — no Storage bucket yet). Live verified in browser: home (hero, theme, bn countdown, 2 upcoming, album card), gallery album page renders 3 photos; photos composite index confirmed via REST. Still ⏳ owner: App Check key, Blaze + Storage, domain.

## 2026-09-04 — v0.21.0 storage LIVE
Owner upgraded to Blaze. Via firebasestorage API (owner OAuth): enabled API, created default bucket ganesh-puja-trust.firebasestorage.app in asia-south1 (matches config). Deployed storage.rules. Live REST: anon read public/* → 404 (allowed), anon read private/* → 403, anon upload → 403. Budget alert ₹100 (50/90/100%) attempted via Billing Budgets API — see next entry for result.

Budget via Billing Budgets API → 404 (billing-account scope); owner sets it in GCP console instead.

## 2026-09-04 — v0.21.1 storage read path verified with a real object
Dummy 600px WebP (canvas via Playwright) uploaded to public/committee/dummy-c2.webp with the owner OAuth token (GCS JSON API), committee/c2.photoUrl set to the Firebase download URL. Anon GET → 200 image/webp 5824B; committee.html renders it (naturalWidth 600). Upload-through-admin-panel still to be tested by the owner on a phone.

## 2026-09-04 — v0.21.2 full demo dataset on production
Owner asked for a full presentation. Generated canvas WebP placeholders (Playwright) → Storage public/{site,history,committee,albums}; Firestore: settings (logo, address, contacts), history 2021–2025 (rich HTML, 7 images), committee ×8 (Storage portraits), albums 2023/2024/2025 × 6 photos, events ×8 (5 upcoming, 3 past). 43 docs via owner OAuth REST (dummy only). Live verified per page in the browser pane (counts + Storage image hosts). All of it is replaceable from /admin/.

## 2026-09-04 — v1.1.0 phase 2–4 rules
TDD (RED→GREEN): firestore.rules gains explicit rules for the six new Phase 2–4 collections — `donations` (public read only `showOnWall==true && deleted==false`, admin all, `phone` field forbidden on write, no delete), `transparency` and `announcements` (public read via `isLive()`, admin all, no delete), `members` (read gated to the owning phone via `isMember()`/`myPhone()`, or admin; no delete), `notices`/`roster` (read gated to `activeMember()` — signed-in phone with an active `members` doc — plus `isLive()`, or admin; no delete) — plus helpers `isMember()`, `myPhone()`, `activeMember()` (the last does a billed `get()` per query, accepted per spec §6.6). tests/rules/_env.js seeds three `members` docs (`+919999999999` active, `+918888888888` active, `+917777777777` inactive) inside the rules-disabled admin seed and returns three new contexts (`member`, `otherMember`, `inactive`) keyed by `phone_number` claim. tests/rules/firestore.test.js gained 5 tests (donations, transparency, announcements, members, notices+roster) — suite now 20/20 green (`npm run test:rules`; storage's 6 unchanged). 3 mandatory mutation checks, each applied/verified/reverted: (1) dropped `showOnWall==true` from donations read → only the donations test failed; (2) dropped the `active==true` clause from `activeMember()` → only the notices+roster test failed (inactive-member assertions); (3) dropped `myPhone()==phone` from members read → only the members test failed (cross-member read succeeded). firestore.indexes.json gained 12 composite indexes (donations ×2, transparency ×2, announcements ×2, notices ×2, roster ×3 incl. `memberPhones` ARRAY_CONTAINS, members ×1) — 9→21 total. No deploy; local files only.

Fix (security review, round 1): removed (soft-deleted) members lose access — `activeMember()` was checking only `active==true`, so an admin soft-deleting a member (`deleted:true`) while `active` stayed `true` left them able to keep reading `notices`/`roster`; now a shared `me()` helper (single cached `get()`) backs `active==true && deleted==false`, and the `members` own-doc read rule itself now also requires `resource.data.deleted == false` (a soft-deleted member can no longer even read their own doc). isLive() tested on notices/roster — new `notices/draft`, `notices/gone`, `roster/draft`, `roster/gone` seeds assert direct-get fails for both an active member and that a `deleted`-only list query (missing `published`) is rejected. donations closed key set — create/update now requires `request.resource.data.keys().hasOnly([...14 allowed fields...])`, closing the nested-object loophole (`{contact:{phone:...}}` bypassed the old flat `!('phone' in data)` check) and any other unlisted key. hasDeletedFlag tested on all six new collections (missing-`deleted` set and non-bool `deleted` update both assert-fail) — previously only asserted on the original four. +4 indexes: `notices`/`roster`/`announcements` (deleted ASC, order ASC — admin list-view shape) and `donations` (deleted ASC, date DESC) — 21→25 total. `isMember()` gained an E.164 shape guard (`^\+[0-9]{10,14}$`) so a malformed `phone_number` claim can't satisfy membership checks. Renamed the stale "unknown collections" test (its target collections gained real rules this same task) to `zzz_unknown/x`, a path genuinely unmatched by any rule. tests/rules/_env.js gained a 4th member `+916666666666` (`active:true, deleted:true`) and context `removed`. Suite still 20/20 green (`npm run test:rules`; 14 firestore + 6 storage). 3 more mutation checks, each applied/verified/reverted: (4) dropped `isLive()` from notices/roster read → only the notices+roster test failed; (5) dropped `deleted==false` from `me()` in `activeMember()` → only the notices+roster test failed (`removed` context wrongly passed); (6) dropped `hasOnly(...)` from donations write → only the donations test failed (nested-phone doc wrongly succeeded). No deploy; local files only.

## 2026-09-04 — v1.1.1 phase 2–4 shared plumbing
TDD (RED→GREEN): new `js/money.js` (pure, no DOM/Firebase) — `sum(items)`, `inr(n, lang)` (Indian grouping via `Intl.NumberFormat('en-IN', { maximumFractionDigits: 0 })`, Bengali digits via `bnDigits` from `js/ui.js` when `lang==='bn'`; Node 24 already produces `12,34,567` grouping, no manual implementation needed), `balance(pledge, payments)`. `tests/unit/money.test.js` (6 tests) — `npm run test:unit` 25→31 green.
`js/i18n.js` gained `nav.donate/transparency/members`, `donate.*` (9 keys), `tr.*` (7 keys), `live.*` (3 keys), `mem.*` (12 keys), `admin.*` for the six new sections (8 keys).
`js/content.js` gained seven reads following the existing `rows()`/try-catch/`console.warn('[content]', …)` pattern: `listDonorWall(limitN=50)`, `listTransparencyYears()`, `getTransparency(year)`, `onAnnouncements(cb)` (realtime `onSnapshot`, published+!deleted, non-expired client-side filter, pinned-then-order sort, `cb(list, {live})`), `getMyMember(phone)`, `listNotices()`, `listMyRoster(phone)` — all query shapes per Task 1's rules (`showOnWall`/`published`+`deleted` gates, `memberPhones array-contains`).
`js/shell.js` NAV gained `donate`/`transparency`/`members` entries (visibility-gated, same as existing sections). `js/firebase.js` re-exports `arrayUnion`, `RecaptchaVerifier`, `signInWithPhoneNumber` (imported from the pinned 12.18.0 CDN modules; `limit`/`onSnapshot` already exported).
`admin/js/upload.js` gained `fileField(ctx, label, currentUrl, { folder, accept='application/pdf', maxBytes=5MB })` mirroring `imageField` minus resize — uploads the raw file via `uploadPublic` with its own `type`, renders a link preview instead of a thumbnail; an oversized file toasts `common.error`, resets `input.value`, and leaves the previous URL untouched (checked before any async work, so the reset is synchronous).
Verified against the Firestore/Auth/Storage emulators (seeded, project `demo-trust`): toggled `settings/site.sectionVisibility.{donate,transparency,members}` via REST (`Authorization: Bearer owner`) — home nav rendered 8 links with the flags `true`, 5 with them `false`. `import('./js/content.js')` in the page confirmed all seven new exports are functions and `onAnnouncements` invoked its callback with `[]`/`{live:false}` on the empty seed; `listDonorWall`/`listTransparencyYears`/`getTransparency`/`listNotices`/`listMyRoster`/`getMyMember` all resolved without error. `fileField` mounted in a throwaway (uncommitted) harness page signed in as the seeded admin: a 165-byte fake PDF uploaded and rendered a link to a Storage emulator URL; a 6 MB blob toasted `common.error` (`.toast-err`), reset the input, and `read()` still returned the prior upload's URL. Emulators, static server, and browser tabs all stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free.

## 2026-09-04 — v1.2.0 donations admin
New `admin/js/sections/donations.js` (pattern: history.js/events.js), registered in `admin/js/admin.js` (dashboard now 7 tiles — `tests/e2e/admin.spec.js` tile count updated 6→7). `#donations`: single query `where('deleted','==',false), orderBy('date','desc')`, grouped client-side by `year` — year `<select>` (years present ∪ current year, default current year or latest present) + summary card (count, `inr(sum())` total, per-mode cash/upi/bank totals, wall count) + list rows `date · name (or donate.anonymous when isAnonymous) · ₹amount · mode` with a wall badge. `#donations/new|<id>`: form with donorName/amount/date/mode(select)/receiptNo(auto-suggested `R-${year}-${Date.now().toString(36).toUpperCase()}` for new only)/isAnonymous/showOnWall(default true)/note; guards empty name, amount ≤ 0, invalid date → toast + return; single Save button (no draft state for donations — always `saveDoc(ctx,'donations', id, data, { publish: true })` with only the closed-set keys); delete via `softDelete`.
Verified against the Firestore/Auth emulators (`demo-trust`) with a throwaway Playwright script (`npm run emu` + `npm run serve`, admin seeded via `npm run seed`): created 3 donations (one `isAnonymous`, one `showOnWall:false`, two different years) — this-year summary count 2 / ₹1,700 total / cash ₹500 / upi ₹1,200 / bank ₹0 / wall 2 correct; last-year summary count 1 / ₹300 / bank ₹300 / wall 0 correct; edited one donation's amount 500→750, this-year total recalculated to ₹1,950 correctly; soft-deleted the last-year donation — it dropped out of the list and its year vanished from the `<select>` entirely (no more active donations that year). REST read-back (`Authorization: Bearer owner`) on all 3 docs: exactly the closed-set keys (`date,mode,donorName,note,amount,isAnonymous,deleted,year,receiptNo,published,showOnWall,order,createdAt,updatedAt`), no `phone` key anywhere. Zero console/page errors throughout. Emulators, static server, and the browser all stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free.

## 2026-09-04 — v1.2.1 donate page
New `donate.html` + `js/pages/donate.js` (pattern: committee.js/events.js — `mountShell('donate', t('donate.title'))`, `sectionVisibility.donate === false` → `common.empty` only, `listDonorWall()` fetched once with `langchange` re-render). Content: UPI card (`s.upiId` set) — big `<code class="upi-id">` id + copy button (`navigator.clipboard.writeText`, guarded when unavailable, `toast(t('donate.copied'))` on success — new i18n key), QR `<img>` when `s.upiQrUrl` is set, and an `<a class="btn" href="upi://pay?pa=<upiId>&pn=<encodeURIComponent(pick(s.name))>&cu=INR">`; else a `.notice` with `donate.soon` + a WhatsApp link when `s.contacts.whatsapp` is set. Confirm card (only when `s.contacts.whatsapp` is set): name/amount/UPI-ref inputs → guards `amount > 0` (toast `common.error`, same pattern as `admin/js/sections/donations.js`'s save guard) → builds `donate.confirmMsg` with `{amount}/{ref}/{name}` replaced (missing ref/name become `—`) → `window.open('https://wa.me/<whatsapp>?text=...', '_blank', 'noopener')`; nothing is persisted. `s.has80G` → `donate.tax80g` line; `s.regNo` → `tr.regNo` line. Donor wall rows (`.donor`): anonymous label or `donorName` (plain string, not bilingual — matches the admin donations list) · `inr(amount, lang)` · `fmtDate(date, lang)`; empty → `common.empty`. `css/site.css` gained `.btn` (not previously present there, only in `css/admin.css`), `.donor` (flex row, bottom border), `.upi-id` (larger, word-break) — per the brief's "check first" instruction.
Verified against the Firestore/Auth emulators (`demo-trust`, base seed via `npm run seed` + a throwaway REST script with `Authorization: Bearer owner`): `settings/site` patched with `upiId 'trust@upi'`, `upiQrUrl` a placehold.co QR, `has80G true`, `regNo 'WB/123'`, `contacts.whatsapp '919800000000'`, `sectionVisibility.donate true`; 4 `donations` docs (2 wall rows — one `isAnonymous` — 1 `showOnWall:false`, 1 `deleted:true`). Browser-driven checks: h1 present; UPI id `trust@upi` shown; QR `<img>` rendered; pay link `href` contains `upi://pay?pa=trust%40upi&pn=...&cu=INR`; copy button (clipboard stubbed via `Object.defineProperty`) wrote `trust@upi` and showed `.toast.toast-ok` "কপি হয়েছে"; confirm form (name `X`, amount `500`, ref `ABC`) with `window.open` intercepted → URL `https://wa.me/919800000000?text=...` decoding to `আমি ₹500 দান করেছি (UPI ref: ABC)। নাম: X`; amount `0` → blocked, `.toast.toast-err` `common.error`, no `window.open` call; donor wall showed exactly 2 `.donor` rows (anonymous one first, newest-date-first), hidden/deleted rows absent; `has80G` line and `Reg. no. WB/123` shown; language toggle → all-English strings (`Donate`, `Pay via UPI`, `Copy`, `Scan the QR`, `Confirm on WhatsApp`, `Tax-deductible under 80G`, `Donor wall`, `Anonymous`); `upiId ''` → `donate.soon` notice + WhatsApp link, no UPI card, confirmed via REST toggle back afterward; 375px mobile viewport `scrollWidth - clientWidth === 0`. Zero console errors from the page itself (the pre-existing, page-independent `identitytoolkit accounts:lookup` 400 on every page load was confirmed present on `committee.html` too, unrelated to this change). `npm run test:unit` 31/31 green (unaffected by this task; no rules/logic-module changes). Emulators, static server, and the browser all stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free.

## 2026-09-04 — v1.2.2 transparency admin
New `admin/js/sections/transparency.js` (pattern: donations.js/history.js), registered in `admin/js/admin.js` after the donations import (dashboard now 8 tiles — `tests/e2e/admin.spec.js` tile count updated 7→8). Doc shape exactly per brief: `{year, income:[{category:{bn,en}, amount}], expense:[...], documents:[{title:{bn,en}, url}], notes:{bn,en}, order:year, published, deleted}`, doc id = year string. `#transparency`: single query `where('deleted','==',false), orderBy('year','desc')` — rows `year · income ₹total · expense ₹total` + draft/published badge, `+ New`. `#transparency/new|<year>`: year field (number, required, disabled on edit — it's the doc id); two dynamic tables built with a local `rowsSection()` helper (biField category + textField amount + ✕ remove + `t('admin.addRow')`), each with a live `inr(sum())` total line recomputed via a single delegated `input` listener on the row list (fires on typing and on add/remove); a live balance line (`tr.balance`) above the documents section recomputes off both tables; documents section (`documentsSection()` helper) — biField title + `fileField(ctx, …, {folder:`public/transparency/${year}`})` + remove; `biField` notes (multiline); Save draft / Publish / Preview (`../transparency.html?year=<y>&preview=1`, edit-only) / Delete (edit-only, via `softDelete`). Guards in a shared `validate()`: year must match `/^\d{4}$/`; every income/expense row's amount must be a finite number ≥0 (toast `common.error` and abort otherwise); empty-category rows are dropped by each table's own `read()`; a documents row with no uploaded URL is dropped by `documentsSection()`'s `read()`. Create-time duplicate guard: `save()` does an extra `getDoc(doc(ctx.db,'transparency',String(year)))` before `saveDoc` when `isNew`, and toasts `common.error` + returns (no write, no navigate) if that year's doc already exists — never silently overwrites.
Fixed a real bug caught during verification, before commit: `rowsSection()`'s own construction called a shared `recompute()` that also invoked the caller's `onAnyChange` (used to refresh the cross-table balance line) — since `const income = rowsSection(...)` and `const expense = rowsSection(...)` are still being assigned while `rowsSection` is constructing the *other* one, `onAnyChange`'s closure over `income`/`expense` hit them in their temporal dead zone and crashed the whole form (`ReferenceError: Cannot access 'income' before initialization`, caught by admin.js's route() try/catch and shown as the generic error state). Fixed by splitting `recompute()` into `updateTotal()` (this table's own total line only, safe to call during construction) and `recompute()` (`updateTotal()` + `onAnyChange()`, only ever invoked later from row-add/remove/`input` events, never during initial construction).
Verified against the Firestore/Auth/Storage emulators (`demo-trust`, seeded via `npm run seed`) with a throwaway Playwright script (`npm run emu` + `npm run serve`): logged in, switched to English, created year 2025 with 3 income rows (চাঁদা 42000, রোড 8000, বাস 5000) and 2 expense rows (প্রতিমা 20000, মণ্ডপ 15000) — live totals showed `Total: ₹55,000` / `Total: ₹35,000` correctly; uploaded a generated 128-byte valid PDF (`%PDF-1.4 … %%EOF`) as document "Audit 2025" — file-link appeared after upload; Save draft → toast + list showed the `Draft` badge; Publish (on the resulting edit view) → toast + list showed `Published`. REST read-back (`Authorization: Bearer owner`): shape matches exactly (year, 3 income entries summing 55000, 2 expense entries summing 35000, 1 document with `title.en` "Audit 2025", `published:true`, `deleted:false`); `documents[0].url` fetched anonymously (no auth header) returned 200 `application/pdf`. Duplicate-year guard: creating a second "new" doc with year 2025 toasted `.toast-err`, stayed on `#transparency/new` (no navigation), and the existing doc's `updateTime` was unchanged before/after — confirmed no overwrite. Oversized-file guard: a 6 MB blob on a documents row toasted `.toast-err` and left no file-link (upload never attempted) — this is `upload.js`'s existing client-side `maxBytes` check, unchanged by this task. Zero console/page errors throughout the whole run. `npm test` (31 unit + 20 rules) green; `npm run e2e` (8 tests, including the updated 8-tile dashboard assertion) green. Emulators, static server, and the browser all stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free; no leftover temp/debug files.

Fix: WhatsApp numbers sanitised to digits (donate + footer); .row on site css; single-pass placeholder replace. `js/ui.js` gained a pure helper `digits(s) => String(s ?? '').replace(/\D/g, '')` (2 new unit tests: `digits('+91 98000-00000') === '919800000000'`, `digits(null) === ''`), used in `js/pages/donate.js` (the `donate.soon` notice's WhatsApp link and the confirm form's `window.open` URL) and `js/shell.js` (footer WhatsApp link) — the WhatsApp link/deep-link now renders only when `digits(s.contacts.whatsapp)` is non-empty, and `wa.me/<digits>` is built from the sanitised value so a settings value like `+91 98000-00000` (spaces/dashes/plus) no longer breaks the `wa.me` URL. `css/site.css` gained `.row { display:flex; gap:.5rem; align-items:center; flex-wrap:wrap; }` (was referenced by `donate.js`'s confirm-form field wrappers but not defined anywhere in this stylesheet — only used unstyled before). `donate.js`'s confirm-message placeholder substitution changed from three chained `.replace('{x}', ...)` calls to a single-pass `msg.replace(/\{(amount|ref|name)\}/g, (_, k) => vals[k])`.
Verified: `npm run test:unit` 33/33 green (31→33). Browser-driven check against the Firestore emulator (`demo-trust`) with `settings/site.contacts.whatsapp` set to `'+91 98000-00000'`: confirm form (name X, amount 500, ref ABC) with `window.open` intercepted → URL `https://wa.me/919800000000?text=...`; footer WhatsApp link `href="https://wa.me/919800000000"`; `.row` computed `display: flex`; zero console errors. Emulators and static server stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free.

## 2026-09-04 — v1.2.3 transparency page
New `transparency.html` + `js/pages/transparency.js` (pattern: donate.js — `mountShell('transparency', t('tr.title'))`, `sectionVisibility.transparency === false` → `common.empty` only). Data: `listTransparencyYears()` fetched once (published+!deleted docs, already carrying the full income/expense/documents/notes fields, no second read needed); when `?preview=1` is present the requested `?year=` doc is also read directly via `getDoc(doc(db,'transparency',yearParam))` (own try/catch, `console.warn` on `permission-denied` so an anon preview request doesn't fail the whole page) and folded into the tab list only if that year isn't already published. Year tabs (`.tabs` buttons, newest first, `?year=` selects if present else the newest) navigate via `location.assign` with the `year` param swapped in (a full reload — simplest way to keep `?preview=1` and other params intact). Selected year's data resolves to the previewed doc when its year matches, else the published list entry, else `null` → `common.empty`; income/expense render as `.table-wrap > table.ledger` (category · `inr(amount)`, plus a `মোট/Total` row via a local `TOTAL_LABEL`), a `.summary` strip (`tr.income`/`tr.expense`/`tr.balance`, `class="neg"` when `income − expense < 0`), documents as `<a target=_blank rel=noopener>` (title only, section omitted when the array is empty), notes via `textContent` (plain, not rendered as rich HTML — matches the brief). Header line combines `tr.regNo <regNo>` and `donate.tax80g` (joined `· `, each half omitted when unset) — new use of an existing key, no new i18n added. `css/site.css` gained `.tabs`/`.table-wrap`/`table.ledger`/`.summary` exactly per the brief's decisions block.
Verified against the Firestore/Auth emulators (`demo-trust`, seeded via `npm run seed`) with a throwaway Admin-SDK script (`tests/seed/_tmp_seed_transparency.mjs`, deleted after the run): `settings/site` patched `sectionVisibility.transparency true`, `regNo 'WB/123'`, `has80G true`; `transparency/2025` published (income 42000+8000+5000 incl. one long category name for overflow testing, expense 20000+15000, 1 document, notes), `transparency/2024` published (income 3000, expense 5000 — expense > income), `transparency/2023` **draft**. Browser-driven checks: default load (no `?year=`) selected 2025, tabs `2025/2024` only (2023 absent); income table showed all 3 rows + `Total ₹55,000`, expense `Total ₹35,000`, summary `Income ₹55,000 / Expense ₹35,000 / Balance ₹20,000` (no `.neg`); document link `href https://example.com/audit-2025.pdf`, `target="_blank"`, `rel="noopener"`; notes text present; header line `Reg. no. WB/123 · Tax-deductible under 80G`. Language toggle → bn: h1 `আয়-ব্যয়ের হিসাব`, tabs `২০২৫`/`২০২৪` (Bengali digits), amounts in Bengali digits, header line in Bengali. Clicking the `2024` tab (in en) navigated to `?year=2024`, selected it (`active` class), showed `Balance: ₹-2,000` with the `.neg` class on that span, documents section omitted (empty array); direct load of `?year=2024` preselected the same. `?year=2023` (draft) as anon: tabs still only `2025/2024`, body `Nothing here yet` (`common.empty`) — no numbers leaked, zero console errors. Logged in at `/admin/` (seeded admin) in the same browser context, then `?year=2023&preview=1`: tab list now included `2023`, draft numbers shown correctly (income ₹1,000, expense ₹500, balance ₹500) with zero console errors on that load (a `console.warn('[transparency] preview', permission-denied)` appears only on the *anon* preview attempt, confirmed via `read_network_requests`/`read_console_messages` scoped to each navigation — never a `console.error`, never surfaced to the anon user as anything but the standard empty state). 375px mobile viewport with the long category name present: `document.body.scrollWidth === document.body.clientWidth` (no page-level horizontal overflow; the table itself scrolls inside `.table-wrap`). `npm test` (33 unit + 20 rules) green; `npm run e2e` (8 tests) green — run correctly this time with `npm run emu` left running + `npm run seed` before `npm run e2e` (the suite doesn't start/seed its own emulators; an earlier same-session attempt without a running emulator produced 4 spurious failures that had nothing to do with this change). Emulators, static server, and the browser all stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free; no leftover temp/debug files.

## 2026-09-04 — v1.3.0 announcements admin
New `admin/js/sections/announcements.js`, registered **first** in `admin/js/admin.js` (`import './sections/announcements.js'; // Phase 3` ahead of `settings.js`, so its tile leads the dashboard — dashboard now 9 tiles, `tests/e2e/admin.spec.js` tile count updated 8→9). Doc shape exactly per brief: `{ text:{bn,en}, pinned, isLive, expiresAt: ISO|'', order: createdAt ms (set once), published, deleted }`. Single route `#announcements` (no sub-routes): a quick-post card (`biField` text multiline, `boolField` pinned, `boolField` isLive, `textField` expiresAt `datetime-local` via `toLocalInput`) with one `t('admin.publish')` button — guards non-empty bn-or-en text (toast `common.error` otherwise), then for a new post `saveDoc(ctx,'announcements', null, {…, order: Date.now()}, {publish:true})`, clears the form, and re-renders the list below; for an in-progress edit (see next) the same card/button instead calls `saveDoc(ctx,'announcements', id, data)` with no `order`/`publish` override, leaving those two fields untouched. List: single query `where('deleted','==','false'), orderBy('order','desc')` (newest first), each row `pick(text) · fmtDate(order, lang)` (display date is the numeric `order`, i.e. creation time, not the Firestore-Timestamp `createdAt` — `fmtDate` accepts a raw ms number natively so this sidesteps ever formatting a Timestamp object) plus badges 📌 (pinned), 🔴 (isLive), `⏳ <date>` when `expiresAt` is set and in the future or the literal "expired"/"মেয়াদ শেষ" when past, and `admin.draft` when unpublished; three `btn-sm` row buttons — Edit (re-opens the quick-post card prefilled, via a shared local `renderForm(cur, id)`), Publish/Unpublish (`updateDoc(ref, {published: !cur, updatedAt: serverTimestamp()})` + `logAudit(ctx, cur?'unpublish':'publish', path, {published:cur}, {published:!cur})`), Delete (`softDelete`). All three row handlers and the form's save handler are wrapped in try/catch with a toast on error (form/delete errors are already toasted inside `saveDoc`/`softDelete`; the publish-toggle handler toasts `common.error` itself since it calls `updateDoc` directly). Field labels (message/pin/live/expires/edit/expired) are local to the section file (`L` object, pattern from `donations.js`) since the brief's toolkit only names `admin.announcements`/`admin.publish`/`admin.unpublish`/`admin.delete`/`admin.saved`/`live.badge`/`common.*` as pre-existing keys — no new `js/i18n.js` keys were needed. No rules or index changes: `announcements` rules (`admin create/update with hasDeletedFlag()`, public `published && !deleted`) and both `(deleted, order DESC)` / `(deleted, order ASC)` indexes already existed from Task 1.
Verified against the Firestore/Auth emulators (`demo-trust`, seeded via `npm run seed`) with a throwaway Playwright script (`tests/seed/_tmp_verify_announcements.mjs`, deleted after the run) plus a throwaway Admin-SDK read-back script (`tests/seed/_tmp_readback_announcements.mjs`, also deleted): logged in, confirmed the dashboard's first tile is `📢 ঘোষণা` (Announcements); posted 3 announcements in sequence — pinned, live, and one with `expiresAt` set to yesterday — the list showed all 3 newest-first (the expired one, posted last, on top) with the correct 📌/🔴/`মেয়াদ শেষ` badges and no `draft` badge (quick-post always publishes). Clicked Unpublish on the top (expired) row → badge set gained `ড্রাফট`; clicked Publish again → badge reverted, confirming the toggle round-trips. Clicked Edit on the pinned row, changed its Bengali text, re-submitted → list showed the updated text with the 📌 badge intact. Deleted the live-message row (confirm + reauth dialogs handled, `password12345`) → it dropped out of the list, leaving 2. REST/Admin-SDK read-back on all 3 docs: exact key set `createdAt,deleted,expiresAt,isLive,order,pinned,published,text,updatedAt` on every doc (matches the brief's required 9 keys exactly); the edited pinned doc's `order` (`1788468913499`) was identical before and after the text edit, confirming edit never touches `order`; the deleted doc showed `deleted:true`. Zero console errors throughout (`page.on('console'|'pageerror')` both empty). `npm run test:unit` 33/33 green (unaffected — no logic-module changes); `npm run test:rules` 20/20 green (unaffected — no rules changes); `npm run e2e` 8/8 green, including the updated 9-tile dashboard assertion. Emulators, static server, and the browser all stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free; no leftover temp/debug files.

## 2026-09-04 — v1.3.1 home live strip
`js/pages/home.js`: two additions to `render()`, module-level `annList = []` / `annLive = false` fed by a new `onAnnouncements((list, {live}) => { annList = list; annLive = live; render(); })` subscription started once after the initial `events`/`albums` fetch succeeds (unsubscribed on `pagehide`) — so realtime pushes reuse the exact same `render()` that `langchange` and the 60 s interval already call, keeping one code path. `.live-strip` renders above `.hero`, omitted entirely (not just hidden) when `annList` is empty: a pulsing `.pulse` chip (`t('live.badge')`) only when `annLive`, then up to 5 `.ann` rows — `📌 ` prefix when `pinned`, `pick(a.text)`, and a trailing `<small>` with `fmtDate(...)` of `createdAt` when present (handles both a Firestore `Timestamp` via `.toDate?.()` and a plain ISO string; omitted when `createdAt` is absent, e.g. immediately after a fresh `serverTimestamp()` write hasn't round-tripped yet). `.today-strip` renders below `.hero`, omitted when no event's `start` falls on today's local date (`toDateString()` comparison against `events`, the same array already loaded for "upcoming"); each row `HH:MM · title · venue` with the time formatted via `toLocaleTimeString(lang==='bn'?'bn-IN':'en-IN', {hour:'2-digit',minute:'2-digit'})`. `content.js`'s `onAnnouncements` already did all the filtering/sorting (published, not expired, pinned-first) — home.js does no additional filtering beyond the 5-row slice. CSS added to `css/site.css`: `.live-strip` (amber background, red left border), `.pulse` (red pill, `@keyframes pulse` 1.2 s opacity fade), `.today-strip` (white card, gold border) — exact rules from the brief.
Verified against the Firestore/Auth emulators (`demo-trust`) with a throwaway Admin-SDK seed script (`tests/seed/_task8-verify-seed.mjs`, deleted after the run — reset `events`/`announcements`, wrote `settings/site`, an admin user + `admins/{uid}` doc, one event today at 18:00 local and one tomorrow) and a throwaway two-context Playwright spec (`tests/e2e/_task8-verify.public.spec.js`, deleted after the run; needed the `.public.spec.js` substring to satisfy `playwright.config.js`'s project `testMatch`). Context B (anon) on `/index.html`: no `.live-strip` present (zero matches, not just hidden) while `.today-strip` showed the 18:00 event. Context A logged in at `/admin/`, then `#announcements`: posted a message with `isLive` checked → in Context B, **without reload**, `expect.poll` found `.live-strip` containing the text and a visible `.pulse` within 5 s; clicked Unpublish on that row → the strip disappeared in B within 5 s (Firestore's per-document rule re-evaluation on `onSnapshot` removes it from the result set the instant `published` flips false, since the public read rule is `published && !deleted`). Posted a plain announcement then a pinned one → B's `.live-strip .ann` showed the pinned one first (`📌` + its text), confirming pinned-first ordering from `content.js` flows through unchanged. Posted a third message, then forced its `expiresAt` to yesterday via a direct Firestore REST `PATCH` (`Authorization: Bearer owner`, which bypasses rules on the emulator) since the admin UI's `datetime-local` picker can't easily express "yesterday" — confirmed it never appeared in B's strip. Toggled `.lang` in B → strip re-rendered with the English text of the pinned announcement (via `langchange` → `render()`, same as every other section). `pageB.on('console'|'pageerror')` captured zero entries for the whole run. One real bug found in the *test itself*, not the app: the very first attempt asserted the pinned-announcement's REST write had landed by immediately `runQuery`-ing right after its toast appeared, which occasionally lost a race against Firestore's read-path catching up to the just-acknowledged write — fixed by retrying that read (10× / 300 ms) rather than by adding app-side delay, per verify-the-verifier discipline. `npm run test:unit` 33/33, `npm run test:rules` 20/20, and the committed `npm run e2e` 8/8 all green afterward (this task touches no rules/index files and no other page). Emulators, static server, and Playwright browser all stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free; no leftover temp/debug files (`tests/seed/_task8-verify-seed.mjs` and `tests/e2e/_task8-verify.public.spec.js` deleted, not committed).

## 2026-09-04 — v1.4.0 phone auth config + seed for phases 2–4
New `scripts/auth-config.mjs` (owner-run, committed — unlike its Phase-1 predecessors, this one must be re-run whenever the custom domain changes): reads `~/.config/configstore/firebase-tools.json` `tokens.refresh_token`, exchanges it against firebase-tools' public OAuth client (`563584335869-…apps.googleusercontent.com`, embedded in the CLI, not a project secret) for an access token, reads the project id from `.firebaserc`, `GET`s `identitytoolkit.googleapis.com/admin/v2/projects/<project>/config`, merges (never replaces) `authorizedDomains` and `signIn.phoneNumber.testPhoneNumbers` with the existing values, then `PATCH`es with `updateMask=signIn.phoneNumber,authorizedDomains`. Refuses to run if the configstore file or its refresh token is missing (`firebase login` first), or if `.firebaserc` can't be read. Optional `--domain <name>` appends one more authorized domain (for the future custom domain — documented in `docs/user-guide/deploy.md`, both the Step 4 walkthrough and the summary table, replacing the old "do it by hand in the console" instruction). Never logs an access or refresh token — only prints `signIn.phoneNumber.enabled`, the (non-secret) test-number/code pairs, and the domain list.

`tests/seed/seed.js` gains the Phase 2–4 demo dataset: `recursiveDelete` now also resets `donations`, `transparency`, `announcements`, `members`, `notices`, `roster`. `members` ×3 — `+919999999999` (active, pledge 5000, two payments totalling 3500), `+918888888888` (active, pledge 3000), `+917777777777` (inactive) — plus an Auth-emulator phone user `auth.createUser({uid:'member-1', phoneNumber:'+919999999999'})` (idempotent via `.catch(() => auth.getUser('member-1'))`, matching the existing admin-user pattern). `notices` ×2 (1 published, 1 draft). `roster` ×2, dated 10/12 days ahead, one row's `memberPhones` including `+919999999999`. `donations` ×4 (2 normal, 1 `showOnWall:false`, 1 `isAnonymous:true`), dates in 2026, `order` = date ms. `transparency/2025` published (income চাঁদা 42000 + রোড শো 8000 + বাস 5000, expense প্রতিমা 20000 + মণ্ডপ 15000, 1 document, notes — same figures verified live in Task 5) and `transparency/2024` draft. `announcements` ×3 (1 pinned, 1 `isLive`, 1 with `expiresAt` in the past). `settings/site` gains `sectionVisibility.{donate,transparency,members}=true`, `upiId:'trust@upi'`, `contacts.whatsapp:'919800000000'`, `regNo:'WB/2026/DEMO'`, `has80G:false` (explicit). Every new doc carries `deleted:false` (via the existing `base` spread) and `published`/`order` per the established conventions (announcements/notices = createdAt ms, roster/donations = date ms, transparency = year, members = 1..n). `console.log` now also prints the seeded member's Auth uid. `tests/e2e/admin.spec.js` needed no change — the dashboard tile count was already 9 (from the announcements-admin task) and this seed adds no admin sections/tiles.

Verified against the Firestore/Auth/Storage emulators (`demo-trust`): `npm run seed` run twice back-to-back with no errors, both runs printing `seeded; admin uid <uid> ; member uid member-1` (idempotent — second run reused the existing Auth user via the `.catch(() => auth.getUser(...))` fallback); REST read-back of `members/+919999999999` confirmed the bilingual name/role, `pledge:5000`, both payment rows, and `deleted:false`. `npm run e2e` (with the freshly seeded emulator + `npm run serve`) 8/8 green, including the 9-tile dashboard assertion. `npm test` (33 unit + 20 rules) green afterward in a separate emulator lifecycle (rules/logic untouched by this task).

Production (`ganesh-puja-trust`, owner-run once via `node scripts/auth-config.mjs`): printed result —
```
signIn.phoneNumber.enabled = true
testPhoneNumbers: { '+919999999999': '123456' }
authorizedDomains: ['localhost', 'ganesh-puja-trust.firebaseapp.com', 'ganesh-puja-trust.web.app', 'hrishi91.github.io']
```
Independently re-verified with a separate one-off `GET` (own throwaway token exchange, not reusing the script's in-process token): `signIn.phoneNumber.enabled === true` and `hrishi91.github.io` present in `authorizedDomains`, matching the script's printed output exactly. No tokens printed or committed anywhere. Emulators, static server, and the browser all stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free; no leftover temp/debug files.
Fix: --domain validated as a bare hostname (isHostname helper + tests); base domains derived from projectId.

## 2026-09-04 — v1.4.1 members admin
New `admin/js/sections/members.js` (pattern: transparency.js/donations.js), registered in `admin/js/admin.js` after the transparency import (dashboard now 10 tiles — `tests/e2e/admin.spec.js` tile count updated 9→10). Doc shape exactly per brief: `{ name:{bn,en}, role:{bn,en}, pledge:number, payments:[{date:'YYYY-MM-DD', amount:number, note:string}], active:boolean, order:number, published:true, deleted }`, doc id = E.164 phone. `#members`: single query `where('deleted','==',false), orderBy('order')` — rows `name · phone · due inr(balance(pledge, payments))` + an `inactive` badge when `!active`, `+ New`. `#members/new|<phone>`: a local `normalizePhone()` helper (`digits(input)` from `js/ui.js`, then 10 digits → `+91`-prefixed, 11–14 digits → `+`-prefixed, else `null`) validated on save (toast `common.error`, no write, stays on the form otherwise) — the phone `textField` is disabled on edit since it's the doc id; `biField` name (required: bn or en) and role; `textField` pledge (number, must be finite ≥0); `boolField` active (default true); a `paymentsSection()` helper (pattern: transparency.js's `rowsSection()`) — date/amount/note `textField` trio per row (date defaults to today) + `t('admin.addPayment')` + ✕ remove, each amount must be a finite number > 0 on save (rows with no positive amount are silently dropped by `read()`, same "blank row from clicking + shouldn't persist" convention as the other dynamic tables) — plus a live `mem.paid`/`mem.due` totals line (`sum()`/`balance()` from `js/money.js`) recomputed via a delegated `input` listener on the rows list and on the pledge field itself. Save → `saveDoc(ctx,'members', phone, data, {publish:true})`; on create, an extra `getDoc(doc(ctx.db,'members',phone))` refuses (toast, no write, no navigate) if that phone already has a doc — never silently overwrites. Delete via the shared `softDelete()` (confirm + reauth, `deleted:true` only — never a hard delete; per `firestore.rules` this also revokes that phone's portal read access, since the member-read rule requires `deleted == false`). No rules or index changes needed: `members` rules (admin create/update with `hasDeletedFlag()`, member-self-read) and the `(deleted, order)` index already existed from Task 9's seed/rules work.
Verified against the Firestore/Auth emulators (`demo-trust`, seeded via `npm run seed`) with a mix of a live Claude-Browser session and a throwaway Playwright script (`tests/seed/_tmp_verify_members_delete.mjs`, deleted after the run — needed only for the confirm+reauth `window.confirm`/`prompt` dialogs the Browser-pane tool can't drive): logged in, dashboard showed 10 tiles ending in `🧾 Members`; `#members` listed the 3 seeded members with correct due values — `Member One +919999999999 Due ₹1,500` (5000−3500), `Member Two +918888888888 Due ₹3,000`, `Member Three +917777777777 Due ₹1,000` with the `Inactive` badge; created a new member with phone `98000 00000` → saved as id `+919800000000` (phone field then shown disabled, value normalised), added 2 payments (₹1,500 + ₹1,000 against a ₹4,000 pledge) → live totals line updated to `Paid: ₹2,500 · Due: ₹1,500` before save and the list showed the same after; invalid phone `12345` → toast `common.error`, stayed on `#members/new`, no document created; duplicate create with `9999999999` (normalises to the existing `+919999999999`) → toast, no navigation, and a direct Firestore REST read confirmed the existing doc's `updateTime` was byte-identical to its `createTime` (no overwrite); edited `+918888888888` to uncheck Active → Publish → list showed its `Inactive` badge; soft-deleted `+918888888888` via the Playwright script (confirm+reauth `password12345`) → dropped out of the list (3 rows remained) and a REST read-back showed the full doc shape intact with only `deleted:true` (and the previously-set `active:false`) changed — a soft delete, not a hard delete. Console/network check on the live session: the only `[error]`-level entry across the whole run was the pre-existing, page-independent `identitytoolkit accounts:lookup` 400 on login (confirmed via `read_network_requests`, same known artifact noted in the v1.2.1 log entry) — zero errors caused by this task. `npm run test:unit` 33/33 and `npm run test:rules` 20/20 green (unaffected — no logic-module or rules changes); `npm run e2e` 8/8 green, including the updated 10-tile dashboard assertion. Emulators, static server, and the browser all stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free; no leftover temp/debug files (`tests/seed/_tmp_verify_members_delete.mjs` and its `_tmp_verify_members_delete2.mjs` companion deleted, not committed).

## 2026-09-04 — v1.4.2 notices + roster admin
New `admin/js/sections/notices.js` and `admin/js/sections/roster.js` (pattern: history.js for the draft/publish/delete form, announcements.js for a custom non-`listView` list query), both registered in `admin/js/admin.js` after the members import (dashboard now 12 tiles — `tests/e2e/admin.spec.js` tile count updated 10→12). Neither uses forms.js's `listView` helper: it only queries `orderBy('order')` ascending with manual up/down reorder, but both collections sort by a derived field instead (notices newest-first by `order`, roster chronologically by `date`), so each section ships its own `listPane()`/`formPane()` split, same shape as members.js.
**Notices** — doc `{ title:{bn,en}, body:{bn,en} (HTML, rendered with DOMPurify on the member page), order: createdAt ms set once, published, deleted }`. `#notices`: `where('deleted','==',false), orderBy('order','desc')`, row = title + draft/published badge, `+ New`. `#notices/new|<id>`: `biField` title (required — biField has no native `required`, enforced manually since "Save draft" is `type=button` and bypasses HTML validation) and `biField` body (multiline), Save draft / Publish / Delete exactly like history.js minus year/images. `order: cur.order ?? Date.now()` on save — **not** a bare `cur.order`: a bug caught during manual verification (see below) where a plain `undefined` on create reached `logAudit`'s `addDoc()` as `after.order === undefined`, which the Firestore JS SDK rejects outright; `logAudit` swallows its own errors so this failed silently (console `audit failed`, no thrown exception, no toast) and simply meant no audit-trail entry was written for every notice creation. Fixed with the same `?? ...` fallback pattern history.js already uses for its own `order` field.
**Roster** — doc `{ date:'YYYY-MM-DD', duty:{bn,en}, memberPhones:string[], note:string, order: date ms (recomputed every save, not set-once), published, deleted }`. `#roster`: `where('deleted','==',false), orderBy('date')`, row = `fmtDate · duty · N Members` + badge. `#roster/new|<id>`: `textField` date (`type=date`, required), `biField` duty (required, both enforced manually), a members checkbox list, `textField` note, Save draft / Publish / Delete. The checkbox list queries `members` with `where('deleted','==',false), orderBy('order')` — matching the existing `(deleted, order)` index — then filters `active` client-side rather than adding a second `where('active','==',true)`, since no `(deleted, active, order)` composite index exists (only `(deleted, order)` and the `(deleted, date)`/`(published, deleted, date)`/`(memberPhones CONTAINS, published, deleted, date)` trio for `roster` itself, all already present from Task 9). Each row shows `name · phone`, pre-checked when the phone is in `cur.memberPhones`; zero members checked is allowed (a duty can be posted unassigned) — only date and duty are required.
No rules or index changes: `notices`/`roster` rules (admin create/update with `hasDeletedFlag()`, member read gated on `activeMember() && isLive()`) and all indexes already existed from Task 9, confirmed unchanged by this task's `npm run test:rules` run (20/20, including the pre-existing `notices + roster` matrix test).
Verified against the Firestore/Auth emulators (`demo-trust`, seeded via `npm run seed`) with a live Claude-Browser session (native `confirm()`/`prompt()` dialogs are suppressed by that tool — overridden in-page via `window.confirm = () => true; window.prompt = () => 'password12345'` for the delete flows, same effect as the Playwright `page.on('dialog', …)` pattern used in `tests/e2e/admin.spec.js`): dashboard showed 12 tiles ending `📋 Notices` / `🗓️ Duty roster`; `#notices` listed the 2 seeded docs (`Puja meeting` published, `Draft notice` draft, newest-first); created a notice, published it — REST read-back confirmed `published:true`, `order` = a fresh `createdAt`-ms timestamp, and (after the `order` fix) a `create` entry actually landed in the `audit` collection; edited its English title and hit Save draft — REST confirmed the new title, `published:false`, and `order` unchanged (preserved, not re-stamped); deleted it — REST confirmed `deleted:true` (not a hard delete) and it dropped off the list. `#roster` listed the 2 seeded rows sorted by date (`13 September 2026 · Gate duty · 1 Members`, `15 September 2026 · Prasad distribution · 1 Members`), each showing only the 2 *active* seeded members as checkboxes (the seeded inactive third member correctly absent from the list); created a roster entry dated 2026-09-09 (+5 days) with both active members ticked — REST confirmed `memberPhones: ['+919999999999','+918888888888']` (both E.164 strings) and `order` = `new Date('2026-09-09').getTime()`; Save draft on it — REST confirmed `published:false`; deleted it — REST confirmed `deleted:true` and it dropped off the list, leaving the original 2. `npm run test:unit` 39/39 and `npm run test:rules` 20/20 green (unaffected — no logic-module or rules changes); `npm run e2e` 8/8 green (`npm run seed` re-run first, since the suite's soft-delete test mutates `history/h1`), including the updated 12-tile dashboard assertion. Zero real console errors introduced by this task (the only `[error]`-level network entries were the pre-existing, page-independent `identitytoolkit accounts:lookup` 400 on login noted in earlier build-log entries). Emulators, static server, and the browser all stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free; no leftover temp/debug files.

## 2026-09-04 — v1.5.0 members portal
New `members.html` (pattern: `about.html` — DOMPurify tag before the module script, since `renderRich()` renders notice bodies here too) plus a sibling `<div id="recaptcha"></div>` right after `<main id="main">`, outside it, so `main.replaceChildren(...)` on every render never touches the RecaptchaVerifier's mount point. New `js/pages/members.js`: `mountShell('members', t('nav.members'))`, gated on `sectionVisibility.members`; a module-local `normalizePhone()` copied verbatim (same 10→`+91`/11–14→`+`/else-invalid rule) from `admin/js/sections/members.js` since the two never share code across the `/admin` boundary. `onAuthStateChanged(auth, …)` drives two views: **logged out** (`user` is null or has no `phoneNumber` — an admin's email-authed session on this page renders the exact same phone form and is never signed out from here) — a phone `<input type=tel>` + `t('mem.sendOtp')`, then (once `signInWithPhoneNumber` resolves) an `.otp` row (`otpInput` + `t('mem.verify')`) that calls `confirmationResult.confirm(code)`; `auth/invalid-phone-number` → toast + refocus the phone field, `auth/too-many-requests` → `admin.tooMany`, anything else → `common.error`, every branch `console.warn`s. **Logged in as a phone user** — `getMyMember(user.phoneNumber)` (null on both "no such doc" and permission-denied, already handled inside `content.js`) → null renders `t('mem.notMember')` + logout; otherwise a card (`pick(name)`, `pick(role)`, a `.stats` 3-tile grid for `mem.pledge`/`mem.paid`/`mem.due` via `inr()`/`sum()`/`balance()` from `js/money.js`, a `fmtDate · inr[· note]` payments list), `t('mem.notices')` (`listNotices()`, each `pick(title)` + `renderRich(pick(body))` in its own `.card`), `t('mem.duties')` (`listMyRoster(phone)`, `fmtDate · pick(duty)[· note]`) — each list falls back to `common.empty` when its array is empty, which for an *inactive* member's notices/roster is exactly what happens: `content.js`'s catch already swallows the `permission-denied` from `activeMember()` failing the rule and returns `[]`, so no extra try/catch is needed in the page. Member data (`member`/`notices`/`roster`) is cached per phone number in module vars so `langchange` re-renders (`renderCard()`/`renderLoggedOut()`) without refetching. CSS added to `css/site.css` exactly per the brief: `.stats` (3-col grid), `.stat` (white tile), `.stat b` (1.3rem), `.otp input` (1.4rem, letter-spaced, centred).
Two implementation bugs, both found and fixed during manual verification, not from the brief:
1. **OTP row stayed visible while "hidden".** The row was built as `el('div', {class:'row otp'}, otpInput, verifyBtn)` with `.hidden = true` set on that same element. `.row`'s `display:flex` and the UA stylesheet's `[hidden]{display:none}` have equal specificity, and the *author* stylesheet (`css/site.css`, loaded after the UA sheet) wins the tie regardless of which file defines it — so the flex layout stayed in effect and the OTP field showed before any code was sent. Fixed by moving the `hidden` toggle to a plain wrapper `<div>` with no layout class of its own, keeping `.row otp` on a child inside it — matches the pattern the `artifact-capabilities` guidance calls out generally ("toggle visibility with `el.hidden`, not `style.display`") but the actual fix here is "don't put a `display` rule on the element you're hiding", not the toggle mechanism itself.
2. **A stale `onAuthStateChanged` invocation could overwrite a newer one's render.** Fixed with a monotonically increasing `authSeq` counter — each invocation captures its own `seq` on entry and checks `seq === authSeq` immediately before touching the DOM, so if a second firing has started (and thus already bumped `authSeq`) by the time an older one's `await loadData(...)` resolves, the older one silently no-ops instead of clobbering the newer state. This surfaced while chasing bug 3 below and is kept as a general defensive measure even after that bug's real cause was found, since `onAuthStateChanged` firing more than once per logical transition is normal Firebase behaviour (e.g. an optimistic update followed by a server-confirmed one) and the guard is free.
3. **Local-only, unresolved: a phone-authenticated session is reliably torn down by the SDK/emulator a few hundred ms after sign-in, on this machine's toolchain.** Manually verified end-to-end once (before this was understood, while `authSeq` above was being added for an unrelated reason): logging in as the seeded active member showed the correct card — `pick(name)` "Member One", `pick(role)` "Secretary", stats ৳৫,০০০/৳৩,৫০০/৳১,৫০০ (bn digits, `pledge=5000` minus the two seeded payments), both payment rows `fmtDate · inr`. Chasing why the notices/roster lists then came back `permission-denied` (rather than the expected 1-row lists) led to isolating the actual defect: it is **not** in `members.js`. A bare page importing nothing but `js/firebase.js` (no application code, no Firestore queries at all — just `RecaptchaVerifier` + `signInWithPhoneNumber` + `confirmationResult.confirm()` + `onAuthStateChanged` logging) reproduces it every time against this repo's Auth emulator: `onAuthStateChanged` fires the correctly-signed-in phone user, then ~1 s later fires `null` — permanently (confirmed with 10+ s waits and full page reloads afterward: nothing is ever persisted to `firebaseLocalStorageDb`, no error is thrown anywhere). Bisected across nine throwaway scratch pages (all deleted, none committed): unaffected by which `Auth` persistence class is used (`browserLocalPersistence`, `indexedDBLocalPersistence`, or the SDK default), unaffected by whether the `RecaptchaVerifier` is reused or `.clear()`-ed immediately after `signInWithPhoneNumber` resolves (a genuine improvement kept in `members.js` regardless — see the `finally` block in `sendBtn.onclick` — since a stale invisible-widget instance being reused is bad practice on its own merits), unaffected by whether `Firestore`'s cache is `persistentLocalCache` (with or without `persistentMultipleTabManager`) or the plain in-memory default, and unaffected by whether the `Firestore` client is even connected to an emulator at all. The one variable that flips the outcome: whether a `Firestore` client for the app exists in the page *at all* — `getFirestore(app)`/`initializeFirestore(...)` called anywhere, even with zero queries ever issued against it, is sufficient to reproduce the null-flip after a phone sign-in; a page with no Firestore import never reproduces it, across every persistence-class variant tried. This points at Firestore's internal auth-credential listener (wired at client-construction time, not at first-query time) interacting badly with this SDK version's (12.18.0) phone-sign-in completion against this firebase-tools Auth emulator version — not at anything `members.js`, `firebase.js`'s Firestore cache config, or the rules layer are doing wrong (the exact same `notices`/`roster` queries, replayed through `@firebase/rules-unit-testing` against a hand-built token with `phone_number:'+919999999999'` and no app code involved at all, succeed and return the expected 1 row each). Since every page on this site needs Firestore, there is no in-scope code fix; `js/firebase.js` was left unchanged (an `indexedDBLocalPersistence` experiment was tried and reverted — it did not fix this, and there is no reason to carry an unrelated persistence-backend change for admin's already-working email flow). Flagged here rather than silently claimed as passing: the brief's "send OTP → verify → reload → still logged in" and the notices/roster row-count assertions could not be demonstrated end-to-end in this local environment. What *was* verified end-to-end: the phone form's send/verify/error-toast wiring, the card's own render logic and i18n/digit correctness (via the one successful login above, and separately via a static DOM injection at 375px confirming the `.stats` grid and payment/notice/duty rows lay out correctly with no overflow), `mem.notMember` + logout for a signed-in phone user with no `members/{phone}` doc, and that `/admin/` email login is completely unaffected by any of the member-portal auth activity on the same origin (logged in fine, same tab, after multiple phone sign-in attempts). This is worth a dedicated follow-up (try a firebase-tools upgrade/downgrade, or test against production's real Phone Auth backend rather than the local emulator, where this class of emulator-only inconsistency may simply not exist).
`npm run test:unit` 39/39 and `npm run test:rules` 20/20 green (no rules/index changes this task). Emulators and static server stopped afterward; ports 8080/9099/9199/5500/4000/4400/4500/9150 confirmed free; no leftover temp/debug files (nine `scratch-authtest*.html` isolation pages, all deleted, none committed).
Fix (round 1, post-review): the review's diagnosis of the verifier's role deserves a correction, not just a comment fix — it argued for constructing the `RecaptchaVerifier` once and reusing it across Resend/Change-number (only `.clear()`-ing on a send failure), to avoid `size:'invisible'`'s `clear()` not emptying `#recaptcha` (a fresh `RecaptchaVerifier` on that non-empty div throws "reCAPTCHA has already been rendered in this element"). Reuse was implemented and network-traced against the emulator: a *second* `signInWithPhoneNumber` call on an already-solved verifier hangs forever — `recaptchaConfig` re-fires, `recaptchaParams`/`accounts:sendVerificationCode` never follow, `sendBtn` stays disabled, no error thrown anywhere. Kept instead (closer to the original v1.5.0 shape, with one addition): the verifier is rebuilt from scratch after *every* send attempt, success or failure — `.clear()`, then explicitly `document.getElementById('recaptcha').replaceChildren()` (the missing piece: `.clear()` alone leaves the div non-empty, which is what actually causes "already rendered", not verifier reuse), then `verifier = null` so `getVerifier()` builds a fresh one next call — verified this handles Resend (same phone, second code appears at the emulator's `verificationCodes` endpoint) and Change-number-then-resend (different phone, no "already rendered" throw, `#recaptcha` never accumulates more than one child) without ever hanging. The `sendOtp()` handler backing both `t('mem.sendOtp')` and the new `t('mem.resend')` button now also shows a field-level `p.err` message (not just a toast) for `auth/invalid-phone-number` and for the client-side normalise failure, added a `t('mem.tooMany')` key so the too-many-requests toast no longer borrows `admin.tooMany`'s namespace, and `verifyBtn` now rejects anything that isn't `/^\d{6}$/` before ever calling `confirmationResult.confirm()`. New `t('mem.changeNumber')` link ('নম্বর বদলান') resets back to the phone step (hides the OTP row, re-enables the phone input/send button, drops `confirmationResult`) without touching the verifier — it's still good for one more send. `css/site.css` gains `.err { color:#b91c1c; min-height:1.2em; }` (site.css had no field-error style; `css/admin.css` already had an equivalent for `#adm-login-err`). Comment at the verifier-reset site rewritten: the old one incorrectly blamed a *reused, un-cleared* verifier for the session-teardown symptom investigated in v1.5.0/v1.5.1 — that symptom is Auth's ProactiveRefresh (armed by Firestore's internal token listener, which calls `getIdToken(true)` shortly after sign-in; when that refresh fails against the emulator, `_logoutIfInvalidated` silently signs the user out) and has nothing to do with this verifier's lifetime either way, per Task 13's investigation. Verified (emulator + throwaway Playwright, deleted after the run): Change-number → resend with a different seeded phone completes with no console error and `#recaptcha` never exceeding 1 child; Resend against the same phone produces a second, distinct verification code at the emulator endpoint and completes sign-in; a 5-digit code shows the error toast and never reaches `confirm()`; an invalid phone (`123`) shows a non-empty `p.err`. `npm run seed && npm run e2e`: 19/19 (unchanged suite — no committed spec references the button text collision between `mem.sendOtp`/`mem.resend`, both containing "OTP পাঠান" as a substring, because the resend button is still `hidden` at the point every existing test clicks the send button). `npm run test:unit` 39/39, `npm run test:rules` 20/20. Emulators/server stopped; ports 8080/9099/9199/5500/4000/4400/4500/9150 free; throwaway `tests/e2e/_task12-fix1-members.spec.js` and a `diag.mjs` scratch script deleted, neither committed.

## 2026-09-04 — v1.5.1 e2e for phases 2–4 + docs
`tests/e2e/{donate,transparency,live,members}.spec.js` (11 new specs: 2+3+2+4) added on top of the
existing 8 (`public.spec.js`, `admin.spec.js`), for 19 total. `donate.spec.js`: donor wall renders
exactly the 3 seeded `showOnWall:true` rows (the plan's Step 1 wording said "2" — the committed
seed actually has 3 via d1/d2/d4; the test asserts the real count), one shown as anonymous, d3's
name never rendered; WhatsApp confirm captures `window.open`'s URL (overridden via
`page.addInitScript`, since a real popup has no route to wa.me in the sandbox) and checks it
contains the encoded amount. `transparency.spec.js`: 2025 totals in bn digits + PDF link
(anonymous); 2024 absent for anonymous; 2024 visible via its own admin login + `?year=2024&preview=1`
(a fresh browser context/admin session inside the test itself, not the shared `admin` project, so
file ordering doesn't matter). `live.spec.js`: pinned-first + 🔴 badge on the home live strip; a
second test opens two `browser.newContext()`s (public + admin), posts a new announcement through
`/admin/#announcements`, and `expect.poll`s the public context for it — no `page.reload()`,
confirming `onAnnouncements()`'s `onSnapshot` really is live. `members.spec.js`: OTP sign-in (code
read from the Auth emulator's `verificationCodes` endpoint) → card/notices/roster for the active
seeded member; an inactive member sees their own pledge/balance but 3× the empty state (payments,
notices, duties — rules gate the latter two on `activeMember()`); a phone with no `members` doc
sees `mem.notMember`; and a real, unskipped "reload → still logged in" assertion.

`playwright.config.js` projects now: `public` (public+donate+transparency+live) → `members` →
`admin` (admin's soft-delete test mutates seed and must run last).

**Investigation verdict (Task 12's "session torn down ~1s after sign-in" concern):** re-tested with
a throwaway Playwright script against the actual committed `members.html`/`js/pages/members.js`
(not isolated scratch pages) — sign in as `+919999999999` via the emulator's OTP code, watch
`onAuthStateChanged` + all `127.0.0.1:9099` traffic for 5s, then reload. **Not reproducible**: 3
repeated trials, plus a variant leaving the `RecaptchaVerifier` deliberately uncleared and a
variant with no Firestore client at all, all stayed signed in the whole window and survived reload,
member card/notices/roster rendering correctly throughout. Most likely explanation: Task 12's own
`v.clear()`-in-`finally` and `authSeq` stale-invocation guard (both already in the committed
`members.js`) already fix the exact symptom described — the report's "Concerns" section was
apparently written against pre-fix isolated scratch pages and never re-verified end-to-end against
the fixed file. Full write-up: `docs/PROJECT_CONTEXT.md` §5, `.superpowers/sdd/.../task-13-report.md`.

`docs/user-guide/admin-guide.md`: dashboard table extended from 6 to 12 cards; six new bn sections
(📢 ঘোষণা, 💰 দান, 📊 হিসাব, 🧾 সদস্য, নোটিশ, দায়িত্ব তালিকা) describing real labels/flows read straight
off the section files. `docs/PROJECT_CONTEXT.md`: new "State as of 2026-09-04" (§5) — what Phases
2–4 add, their decisions-and-causes, the investigation writeup, e2e coverage summary.
`docs/pending.md`: Phase 2–4 Tasks 1–13 ticked, Task 14 pending (owner-driven, same blocker class as
Phase 0's Task 11), deferred minors from this phase's reviews listed, "Later phases" narrowed to the
payment-gateway idea (post-80G).

`npm run seed && npm run e2e`: 19/19 green, twice. `npm test` (39 unit + 20 rules): green, unchanged
(no rules/index changes this task). All emulator/server background processes stopped; ports
8080/9099/9199/5500/4000/4400/4500/9150 confirmed free; throwaway investigation files
(`scratch-authtest-*.html`, `scratch-investigate*.mjs`) deleted, none committed.

## 2026-09-04 — v2.0.0 phases 2–4 LIVE

Task 14, the production rollout for donations/transparency/live announcements/members, is done —
`ganesh-puja-trust` now serves the full Phase 2–4 feature set for real.

**Step 1 — rules + indexes.** `npm test` (39 unit + 20 rules) green, then `firestore.rules` (the
`donations`/`transparency`/`announcements`/`members`/`notices`/`roster` matches, including the
`donations` closed-key-set write guard and the `!('phone' in request.resource.data)` check) plus
all 25 composite indexes in `firestore.indexes.json` deployed via `firebase deploy`. Verified live:
`npx firebase firestore:indexes --project ganesh-puja-trust | grep -c collectionGroup` → 25; a
REST `runQuery` on `announcements` (`published==true, deleted==false`, `order desc`) returned rows
with no "requires an index" error.

**Step 2 — auth config.** `node scripts/auth-config.mjs` already applied earlier this session
(logged under v1.4.0 above): phone sign-in enabled, test number `+919999999999` → `123456`,
`hrishi91.github.io` in `authorizedDomains` — re-confirmed still in place before writing demo data.

**Step 3 — demo data.** Written to `ganesh-puja-trust` via a throwaway `.tmp-demo2.mjs` (owner-OAuth
REST pattern: firebase-tools' refresh token exchanged against its public client for an access
token with Owner/Editor IAM — bypasses Firestore/Storage security rules the same way the Phase-1
one-off scripts did; deleted immediately after the run, no token ever printed or committed):
- `settings/site` merge-patched (`updateMask` on exactly these fields, nothing else touched):
  `upiId: 'ganeshpujatrust@upi'` (dummy), `upiQrUrl` → a generated 512×512 "DEMO QR — NOT
  SCANNABLE" placeholder (Playwright `chromium`, canvas → WebP, 6572 B) uploaded to
  `public/site/upi-qr.webp`, `regNo: 'WB/2026/DEMO'`, `has80G: false`, `contacts.whatsapp` (already
  `919800000000` from earlier seeding — confirmed unchanged).
- `donations` ×12 (`d1`–`d12`), 2025 (8) + 2026 (4), realistic Bengali donor names, modes mixed
  cash/upi/bank, `receiptNo` `R-<year>-NNN`, `order` = date ms, `year`, `published:true,
  deleted:false, note:''`; 3 `isAnonymous:true` (d3, d6, d10), 2 `showOnWall:false` (d4, d8) —
  exactly the closed key set the rules allow, verified by REST read-back (no `phone` key, no
  extras).
- `transparency/2024` and `/2025`, both published: income চাঁদা/রাস্তার সংগ্রহ/বাস-টোটো/দান, expense
  প্রতিমা/মণ্ডপ/আলো/ভোগ/সাংস্কৃতিক. 2025 totals ₹2,40,000 income / ₹2,10,000 expense (surplus
  ₹30,000) exactly as speced; 2024 a smaller plausible ₹2,00,000/₹1,80,000. One generated PDF each
  (`buildMinimalPdf()` — hand-built objects + correct byte-offset xref table, `%PDF-1.4` header,
  693 B each, well over the 300 B floor) uploaded to `public/transparency/<year>/audit-<year>.pdf`
  as `documents[0]`, bilingual notes, `order` = year.
- `announcements` ×4 (`a1`–`a4`): `a1` pinned welcome message, `a2` `isLive:true` "মণ্ডপে আরতি
  চলছে", `a3` a plain notice, `a4` `expiresAt` = yesterday; `order` = staggered creation ms,
  `published:true`.
- `members` ×5 more (`+919800000001`–`05`, DEMO numbers only — never a real phone), 1
  (`…05`) `active:false`; existing `+919999999999` (pledge 5000, two payments) untouched.
- `notices` ×2 more (`n2` published, `n3` draft) alongside the existing `n1`.
- `roster` ×3 more (`r2`/`r3`/`r4`, dated 2026-09-14/15/16 around the 2026-09-15 puja date, 2–3
  member phones each) alongside the existing `r1`; `+919999999999` included in `r2` and `r3` (two
  rows, per spec).
Per-collection counts printed by the script: `donations: 12, transparency: 2, announcements: 4,
members: 5, notices: 2, roster: 3`. `.tmp-demo2.mjs` deleted immediately after the run.

**Step 4 — live verification**, all against `https://hrishi91.github.io/trust_webpage/…` (freshness
gate passed first: `curl -s .../js/pages/members.js | grep -c changeNumber` → 1, confirming commit
`1b8c032` was already live):
- **Donate**: UPI card renders `ganeshpujatrust@upi` + the uploaded QR `<img>`; donor wall shows
  exactly the 10 non-hidden rows (12 − 2 `showOnWall:false`), anonymous ones labelled "নাম প্রকাশে
  অনিচ্ছুক" instead of a name; registration number shown. Console clean.
- **Transparency**: 2025 tab (default) shows income ₹2,40,000 / expense ₹2,10,000 / surplus
  ₹30,000, matching the seeded rows exactly; the audit PDF link resolves 200 `application/pdf`
  (confirmed both via `curl -sI` and by locating the link in the rendered page).
- **Home**: live strip shows the 🔴 badge (from `a2`'s `isLive:true`) with the pinned announcement
  (`a1`) first, the expired one (`a4`) correctly absent; posting a fifth announcement via REST
  (`a5-verify`) appeared in the strip on the already-open tab with **no reload** (Firestore
  `onSnapshot` push), then was soft-deleted via REST and confirmed gone from the strip, again
  without reload — realtime round-trip verified both directions. No event falls on 2026-09-04
  itself in the existing (Phase-1) events data, so no today-strip is expected today and none
  rendered — consistent, not a bug.
- **Members**: `members.html` renders the phone-entry form and "OTP পাঠান" send button; OTP was
  deliberately not attempted (reCAPTCHA-gated — the owner tests the real flow on a phone with the
  `+919999999999`/`123456` test pair).
- **Admin**: `/admin/` loads and (via this session's already-authenticated browser profile) showed
  the 12-tile dashboard, confirming the panel is fully live; no separate anonymous-session check
  was done since the login-form render itself was already exercised by Tasks 1–13's e2e suite.
- **Mobile**: donate/transparency/home/members/admin all measured `scrollWidth === innerWidth` at
  375×700 (no horizontal overflow).
- **Console**: zero errors across every page checked (no App Check warning surfaced during this
  run's window, though one is expected per the brief and would not have been a concern). Two
  stale error/warning lines observed early on turned out to be console-buffer bleed-through from a
  previously-reused browser tab (a members.js OTP failure and a reCAPTCHA-Enterprise log from an
  *earlier, unrelated* session) — re-verified against a freshly opened tab, which showed zero
  console entries on every page; not a defect in this rollout.

**Step 5 — docs.** This entry; `docs/pending.md` Task 14 ticked and Phase 2–4 section marked done;
`docs/PROJECT_CONTEXT.md` gains a one-line "Live: phases 2–4 on 2026-09-04" note; `README.md`
status line updated. Commit `docs: phases 2-4 live on production`, tag `v2.0.0`, pushed with tags.

No background processes left running, all emulator/dev ports free (nothing was started this task —
verification ran against production only), no `.tmp-*` files left in the repo.

## 2026-09-04 — v2.0.1 post-review hardening

Single fix wave from the final whole-branch review of Phases 2–4 (production LIVE, tag `v2.0.0`).
One group per commit; see each subsection below.

**G1 — test phone number must not be re-added silently.** `scripts/auth-config.mjs` no longer
merges `{'+919999999999':'123456'}` into `testPhoneNumbers` unconditionally — without a flag it
now leaves whatever `testPhoneNumbers` the server already has untouched (neither adds nor
deletes). `--with-test-number` merges the fixed demo pair in; `--remove-test-number` removes it
(the go-live step, before real members are loaded — if both flags are passed together, removal
wins). The script now prints only the test numbers themselves, never their OTP codes. Arg parsing
extracted to `scripts/lib/args.mjs` (`parseArgs(argv) → {domain, withTestNumber,
removeTestNumber}`), unit-tested in `tests/unit/args.test.js` (7 cases: defaults, `--domain` with
and without a value, each flag alone, combined, order-independence). `docs/user-guide/go-live-checklist.md`
gains a "Step 4 — Members portal: real members + OTP" section: the `--remove-test-number` +
console-check owner step, an OTP-login-test bullet, and a note that the production SMS region
policy was set to India-only on 2026-09-04. The script itself was NOT run against production this
task (owner-run only, per the brief).

**G2 — read failures must not look like empty data.** `js/content.js`'s eight read helpers
(`listDonorWall`, `listTransparencyYears`, `listNotices`, `listMyRoster`, `getMyMember`,
`getTransparency`, `listPhotos`, `getPublished`) used to swallow every read error into their
`[]`/`null` fallback. New shared `isExpectedAccessError(err, {allowNotFound})` narrows that to just
`permission-denied` (the array-returning helpers) or `permission-denied`/`not-found` (the
single-doc getters) — those stay silent (an inactive member's notices/roster query, or a signed-out
visitor hitting a gated doc, are legitimate access outcomes the caller already renders as empty).
Everything else — `failed-precondition` (a missing composite index), `unavailable`, any other
error — is `console.warn`ed and then rethrown. `donate.js`, `transparency.js`, `gallery.js`,
`about.js`, `home.js`, `events.js`, `committee.js` already wrap their content.js calls in
try/catch → `common.error`, verified unchanged. `js/pages/members.js`'s `onAuthStateChanged`
handler did NOT: a `loadData()` failure was swallowed to a `console.warn` and then `renderCard()`
ran anyway with `member` still `null`, rendering the misleading "this number is not on the members
list" message for what could be a real outage. Fixed to show `common.error` instead when
`loadData()` throws. No index changes needed to reproduce/verify this on the emulator (a genuine
`failed-precondition` needs a missing index, which the emulator doesn't enforce the same way
production does) — verified indirectly: `npm run seed && npm run e2e` stays green, i.e. the
narrowed catch does not turn the existing legitimate-empty-list cases (inactive member's
notices/roster, anonymous donor wall reads, etc.) into false errors.

**G3 — roster index insurance.** `firestore.indexes.json` gains a second `roster` composite index
with the equality fields first (`published ASC, deleted ASC, memberPhones CONTAINS, date ASC`),
alongside the existing `memberPhones CONTAINS, published ASC, deleted ASC, date ASC` shape — cheap
insurance against Firestore's query planner preferring a different field order than the one
already declared for `listMyRoster()`'s `array-contains` + two-equality + orderBy query.
`docs/user-guide/go-live-checklist.md`'s new OTP-login-test bullet (added under G1) gets a note:
if the first real OTP login shows a FAILED_PRECONDITION/index link in devtools console, create
that index. `firestore.rules` unchanged this task; index-only change, `npm test` unaffected.

**G4 — rules intent comment + docs typo.** `firestore.rules` gains a comment above the
`notices`/`roster` matches making explicit what the rules already do: any active member may read
all published notices and the whole roster, so `memberPhones` is visible to every active member
(spec §5) — this was implicit in the rule bodies, not stated anywhere. `docs/PROJECT_CONTEXT.md`:
fixed the "Live: phases 2–4" line's project id typo, `ganeshpuja-trust` → `ganesh-puja-trust`
(matches `.firebaserc`'s actual `projects.default` and every other reference in the repo).
Comment-only rules change — `npm run test:rules` (20) confirmed still green, unchanged.

**G5 — SHOULD-FIX-SOON minors.** (a) `normalizePhone` was duplicated in `js/pages/members.js` and
`admin/js/sections/members.js` and neither rejected a leading zero after normalisation — an
11-digit input starting with `0` (e.g. `'09800000000'`, a domestic dialing habit) silently became
the nonsensical `+09800000000` instead of being refused. Extracted to `js/phone.js`
(`export function normalizePhone(input)`, built on `digits()` from `js/ui.js`) with the leading-
zero check added up front; both call sites now import it instead of keeping a local copy.
`tests/unit/phone.test.js` (5 cases: 10-digit with spaces, `+91`-prefixed with punctuation,
leading-zero rejection, too-short rejection, 12-digit no-`+`). (b) `admin/js/upload.js`'s
`fileField` (used for transparency PDF documents) now slugifies `file.name`
(`.toLowerCase().replace(/[^a-z0-9.]+/g,'-').replace(/^-+|-+$/g,'')`, dots kept so the extension
survives) before it goes into the Storage path — an admin's original filename can carry spaces,
uppercase, or other characters awkward in a Storage/GCS path; the displayed link text still shows
the original `file.name`, only the path is slugified. (c) `scripts/auth-config.mjs` printing
numbers-only was done under G1. (d) `tests/e2e/members.spec.js` gains three specs: change-number
(send OTP → click "নম্বর বদলান" → phone field re-enabled, OTP row hidden), resend (click "আবার OTP
পাঠান" → a second verification code appears at the Auth emulator's `verificationCodes` endpoint
for that phone), logout (click "লগআউট" → back to the phone-entry form). `npm run test:unit` is now
51 (39 + 7 from G1's `args.test.js` + 5 from this group's `phone.test.js`); `npm run seed && npm
run e2e` is 22/22, run twice, both green (19 existing + 3 new).

## 2026-09-10 — design reference scan (Phase 5 prep)
Owner asked for 5 selectable modern designs, admin-chosen. Scanned lalbaugcharaja.com, gsbsevamandal.org, mumbaicharaja.co, akshayapatra.org, giveindia.org, goonj.org + our site; findings and five token-system directions (Utsav, Nirmal, Ratri, Mati, Prabhat) in docs/design/reference-scan-2026-09-10.md. No code change.

## 2026-09-10 — design thinking doc + theme sample
docs/design/design-thinking-2026-09-10.md: culture→design-material table (alpona, marigold, kumkum, diya, kantha/gamcha, Ganesh iconography, Malda mango/silk/Gambhira), 2026 UI patterns kept/dropped, shared skeleton wireframe, five themes as decisions with one signature each, self-critique, a11y/perf floor. Visual sample (5 hero mockups, real Google Fonts, real bn copy) published as an Artifact for the owner to review.

## 2026-09-10 — five design proposals (canvas)
Owner correction: the Trust is in Dakshin Dinajpur (Balurghat), not Malda — earlier docs said Malda from the Chanda app side names; fixed in the proposals. Research: Ganesh iconography colours (sindoor, pitambar, durva, modak) and district crafts (Kushmandi wooden mask GI 2018, Dhokra, Khon, Bhawaiya, Bangarh terracotta, Atreyee). Five proposals as Claude Design artboards in docs/design/proposals/ (Sindoor, Mukha, Dhokra, Atreyee, Bangarh) with motivation/trade-off notes; published as a canvas for the owner to pick from.

## 2026-09-10 — full homepage concept + page-pattern research
Owner: "not using full intelligence" — wireframe-with-colours was not a design. Built a complete homepage concept (hand-drawn Ganesh line art SVG, marigold garland, diyas, mandala/mask/coil/river/frieze canvas art per theme, culture cards for Kushmandi mask / Dhokra / Atreyee, schedule, ledger, donate band with UPI QR, members teaser) with a 5-way switcher — published as an Artifact. Then full-page + inner-page screenshots (desktop + phone) of Siddhivinayak, Belur Math, Lalbaugcha Raja, GSB Seva Mandal, Akshaya Patra, GiveIndia → docs/design/page-patterns-2026-09-10.md: per-page anatomy of each site and our page-by-page plan (about timeline, committee, gallery albums, events tabs, donate purpose cards, transparency legal block, members, admin design card) + phone rules + Phase 5 scope.

## 2026-09-10 — full-site concept (all pages, one presentation)
Owner: "take it as one" — the homepage concept direction is the one. Extended it into every public page inside a single file with a hash router (nav switches ইতিহাস timeline · কমিটি officers+grid · গ্যালারি best strip+albums · অনুষ্ঠান day tabs · দান UPI+purpose cards+WhatsApp+donor wall · হিসাব year tabs+ledger+donut+documents+legal block · সদস্য OTP+dashboard), each page with its own mandala-glow header canvas and hand-drawn scene art; the five theme token sets still switch live. Verified with Playwright at 1366 and 390 (no console errors). Source kept as docs/design/concept/full-site-concept.html (README explains); republished to the same Artifact URL. Next: Phase 5 spec addition + plan (theme engine, art component, page restructure, admin 🎨 ডিজাইন card).

## 2026-09-10 — Phase 5 spec addition + implementation plan
docs/superpowers/specs/2026-09-10-phase-5-design-system.md (theme stored as settings.design — `theme` already means "this year's theme"; rules whitelist; ?theme= preview never persists; localStorage cache + inline head script against FOUC; ticker moves into the shell; new flags officer/featured/donatePurposes/culture) and docs/superpowers/plans/2026-09-10-phase-5-design-system.md (13 tasks: theme.js → rules → CSS+contrast test → art.js → shell → home ×2 → about/committee/gallery → events/donate → transparency/members → admin fields → 🎨 card → shots+docs+deploy). Execution: subagent-driven with per-task review, ledger under .superpowers/sdd/.

## 2026-09-10 — Phase 5 Task 1 — js/theme.js + head cache script
Pure theme resolver (THEMES, DEFAULT_THEME, THEME_META, resolveTheme, isPreview, applyTheme); inline head script (localStorage-backed, no FOUC); eight public HTML pages wired with Google Fonts + themes.css link. 5 new tests (56 total).

## 2026-09-10 — Phase 5 Task 2 — settings.design rule whitelist
TDD (RED→GREEN): firestore.rules gains `validDesign()` helper (permit missing design or one of five theme names: siddhi, mukha, dhokra, atreyee, bangarh) and `match /settings/site` now gates on `isAdmin() && validDesign()`. tests/rules/firestore.test.js gained one test (21 total, 20→21 pass). No deploy (Task 13).

## 2026-09-10 — Phase 5 Task 3 — theme tokens + component CSS + contrast test
TDD (RED→GREEN): tests/unit/contrast.test.js added first (RED — ENOENT css/themes.css), then css/themes.css created (four `[data-theme]` blocks, concept lines 14–17 verbatim, dhokra gains `color-scheme:dark;`) and css/site.css fully rewritten from concept lines 5–13 (siddhi tokens + `--focus`/`color-scheme:light`) + 19–297 (base→members, skipping line 199's `.page{display:none}` and the 298–302 switcher) + a bridge block for class names the JS still emits (`.tabs`, `.summary`, `.stats`, `.donor`, `.lightbox`, `table.ledger`, `.otp input`, `.countdown`). No token adjustments needed — all five themes passed the contrast floors (ink/muted vs ivory ≥4.5, hero-ink/bg ≥4.5, cta-ink/cta ≥3.0, ink/card ≥4.5) on the first run. 58 unit tests total (56→58), all green.
Fix round 1: bridge block was re-declaring concept values (`.stat b`, `.notice`, `.otp input/.form input` with different properties) instead of only adding missing ones. Deleted duplicates (`.stats`, `.stat`, `.stat b`, `.notice`, `.person{ }`); replaced `.otp input,.form input` with only the three properties missing from concept (width/min-height/color). Contrast test + full unit suite (58) still green.

## 2026-09-10 — Phase 5 Task 4 — js/art.js
Ported the Ganesh line-art SVG + one diya SVG (concept lines 338–380) and `setup`/`rng`/`css`/`heroBg`/`garland` (lines 672–722) + `phBg` (756–758) into `js/art.js` as `ganeshSvg`/`diyaSvg`/`paintHero`/`paintGarland`/`paintHeader`/`onResize`, via `DOMParser` (not innerHTML) per the no-innerHTML-outside-rich.js rule; `scene()` (723–755) not ported (Task 6+ uses real photos). Node import check printed all six export names with no throw. Fixed a real bug found while verifying: the concept's inline `<svg>` relies on the HTML parser's implicit foreign-content namespace switch, so parsing the same string standalone via `DOMParser(...,'image/svg+xml')` produced `namespaceURI: null` (invisible, zero-size) elements — added `xmlns="http://www.w3.org/2000/svg"` to both SVG constants, verified fixed via a scratch Playwright check (outside the repo) at `?theme=siddhi` and `?theme=mukha`: siddhi shows the gold Ganesh line art (crown/curled trunk/modak/mouse), three lit diyas, mandala-ring + gold-bokeh hero background, and marigold-dot garland; mukha shows the same Ganesh/diyas in ink-outline, a carved hexagonal mask-tessellation hero background with terracotta/durva diamond accents, and the same garland — no console errors either run. Full suite green (58 unit + 21 rules).
Fix round 1 (2026-09-10): `onResize` unsubscribe leaked the pending debounce timer; added `clearTimeout(t)` to the returned unsubscribe function. Removed unused `pit` destructure from `paintHero` (line 67) with comment.

## 2026-09-10 — Phase 5 Task 5 — shell
TDD (RED→GREEN): tests/e2e/theme.spec.js added first (RED — no `data-theme`, no `.nav .brand`), then js/shell.js rewritten (applies theme via resolveTheme/applyTheme/isPreview, renders a site-wide `.ticker.live-strip` announcement ticker, sticky `.nav` with brand/links/lang/burger, four-column footer, plus new exports `section`/`sectionHead`/`pageHeader`); DEFAULT_SETTINGS in js/content.js gains `design`/`donatePurposes`/`sectionVisibility.culture`. Fixed a self-found bug: the marquee's `[0,1].flatMap` duplication (needed for the seamless CSS loop) was also duplicating the `.pulse` live badge, breaking any single-element `.live-strip .pulse` locator — now rendered only in pass 0. theme.spec's first two tests pass; its third (`.ph h1` on about.html) is split into its own test and fails on purpose until Task 8 wires about.html to `pageHeader()`. live.spec.js (unchanged) still fails both tests: home.js's own pre-existing `<section class="live-strip">` (Task 6's to remove) now collides with the new shared ticker, which also carries `.live-strip` by design — both elements match the same selector. 58 unit tests still green.
2026-09-11 fix round 1 (review): `render()` rebuilt the whole `#site-header` on every announcements snapshot, silently closing an open mobile menu — split into `renderTicker()`/`renderNav()`/`renderFooter()`; `onAnnouncements` now calls `renderTicker()` only (replaces/inserts/removes just the `.ticker` as `#site-header`'s first child), `onLangChange` still calls all three. Burger touch target was ~26px — `.nav .burger` bridge rule gains `min-height/min-width:44px` and its `@media(max-width:860px)` override switches `display:block`→`display:grid;place-items:center` (edited the existing rule, no new conflicting one). Burger `aria-label` now `pick({bn:'মেনু',en:'Menu'})` instead of hardcoded `'Menu'`. Verified: `theme.spec.js` 2/3 pass (`.ph h1` red until Task 8, as before); a scratch Playwright script opened `about.html` at 390px, clicked `.burger` (menu opened), posted a fresh announcement via the Firestore emulator REST API to force a ticker re-render, and confirmed `.links.open`/`aria-expanded=true` survived, plus `.burger` bounding box is 44×44 — all passed; `npm run test:unit` 58/58 green; reseeded after the REST insert.

## 2026-09-11 — Phase 5 Task 6 — home part 1
Rewrote `js/pages/home.js` (hero with `ganeshSvg()`/painted canvas bg + garland, credibility strip, `bento` at-a-glance grid, donate band, members teaser); removed the old page-owned `.live-strip` section now that the shell renders the ticker (fixes the Task 5 collision — `home shows the pinned announcement…` in live.spec.js is green again). Added `js/culture.js` (CULTURE district facts) and `js/ledger-view.js` (barsView/donutView stubs) for Task 7/8 to fill in; six `story/culture/gallery/schedule/ledger/committee` render stubs kept as `() => null` per the brief. Screenshots at 390px and 1366px (Playwright, fresh browser context) show the gold-line Ganesh art glowing over the mandala hero background, a 3-part countdown, the marigold garland strip below the hero, credibility strip, bento collapsing to one column on mobile with no horizontal overflow, donate band with the UPI code, and the members OTP form.
Remaining red (expected, out of this task's scope): public.spec.js's home test still fails on the gallery-album link (gallery() is a stub until Task 7); live.spec.js's admin-announcement test fails because the shell's marquee intentionally duplicates each `.ann` node for the seamless CSS loop, so a text-matched count is 2 not 1 — pre-existing shell.js behavior, unrelated to home.js, left for a follow-up.
Fix round 1 (2026-09-11): `hero()` called `onResize()` on every `render()` without keeping the unsubscribe, leaking a resize listener each time — fixed with a module-level `unsubHero` guard, `tests/e2e/live.spec.js`'s duplicate-badge assertion loosened to `.toBeGreaterThanOrEqual(1)` (shell ticker duplicates each `.ann` for the seamless marquee), `live.spec.js`+`public.spec.js` reconfirmed green (6/7, 1 expected red until Task 7) and a scratch listener-leak check stayed flat across three lang toggles.

## 2026-09-11 — Phase 5 Task 7 — home part 2
TDD (RED→GREEN): `tests/unit/ledger.test.js` added first (RED — module missing), then `js/ledger.js` (pure `barWidths`/`donutArcs`/`parsePurposes`) written; 5/5 green (63 unit tests total, 58→63). Filled `js/ledger-view.js` (`barsView`/`donutView`, SVG donut with two rotated stroke-dasharray circles). Replaced the six `home.js` stubs (`story`, `culture`, `gallery`, `schedule`, `ledger`, `committee`) with the brief's exact code; added `renderRich` import and the DOMPurify `<script>` tag (copied from about.html) to `index.html`. CSS: added the three brief-listed rules (`.ccard .ill[data-kind]` radial gradients per kind, `.people .ring img` object-fit, `.ev.live` border) plus bridge rules for `.masonry a`/`.masonry a img`/`.masonry a.big`/`.masonry a .cap` — the concept's `.masonry` block only styled `figure`+`canvas` children, but the actual gallery markup (this task's exact code) uses `a`+`img`+`span.cap`, so without these additions the images were unclipped and the caption never overlaid.
Verification: `npx playwright test tests/e2e/public.spec.js tests/e2e/live.spec.js` — 7/7 green, including the previously-red gallery-album-link assertion. Screenshots (Playwright, fresh browser, outside repo) at 390px and 1366px confirm the theme story, three culture cards (each with a distinct token-coloured gradient), gallery masonry tile, schedule row, ledger bars + 57% donut, and committee ring row all render correctly with no horizontal overflow. Emulators left running.
Fix round 1 (review, same day): `schedule()`'s live check collapsed to an instant for events with an empty `end` (`new Date(e.end || e.start)` made the window zero-width), so `.ev.live` never showed for typical events — added `isLiveEvent(e, now, defaultMs=2h)` to `js/ui.js` (pure section; TDD RED→GREEN, 4 new tests in `tests/unit/ui.test.js`, 63→67 unit tests) and `home.js`'s `schedule()` now calls it instead of the inline expression. `ledger()` passed the top-4 `topE` subset to `donutView`, so the ring always drew 100% of the subset while the centre % was computed from the full expense total (disagreeing whenever more than 4 expense categories exist) — `donutView` now receives the full `exp` list so ring and centre % agree; `topE` still drives the bars and the centre label. Verified: `node --test tests/unit/ui.test.js` RED (missing export) then GREEN; `npm run test:unit` 67/67; emulators already running (8080/9099), reseeded, `npx playwright test tests/e2e/public.spec.js tests/e2e/live.spec.js` 7/7 green. Emulators left running.

## 2026-09-11 — Phase 5 Task 8 — about/committee/gallery
Rewrote the render blocks of `js/pages/about.js` (history sorted newest-first into a `.tl` year-rail + card timeline), `js/pages/committee.js` (`.officers` ring cards for `p.officer`, `.mgrid` rows for the rest, with `person` kept as a second class per card so the pre-existing `.person` e2e count still holds), and `js/pages/gallery.js` (`.best` featured strip — empty until Task 11 adds the `featured` flag — plus `.albums` tiles with `.nocover` fallback, and the single-album view moved into `pageHeader`+`.masonry`). Deviated from the brief once: about's `pageHeader` title is `t('nav.about')` (not `pick(s.name)`), so `.ph h1` reads "ইতিহাস" as `tests/e2e/theme.spec.js`'s pre-existing (Task-5-authored) assertion expects — the trust name already sits in the nav brand. `tests/e2e/public.spec.js` updated first (RED: `.albums a` and `.officers .officer, .mgrid .mrow` selectors didn't exist yet), then the three pages rewritten to GREEN. CSS: added the brief's two `.album`/`.best` image rules plus one unlisted bridge rule, `.masonry img.cover` (the concept `.masonry` styles `figure`+`canvas`, Task 7 added `a`+`img` for the home page, but the gallery album-photo view renders bare `<img class="cover">` — without a rule it had no clipping/object-fit).
Verification: `npx playwright test tests/e2e/public.spec.js tests/e2e/theme.spec.js` 8/8 green (including the about `.ph h1` test, red since Task 5); full `npx playwright test` 25/25 green; `npm run test:unit` 67/67. Screenshots (fresh Playwright browser, outside repo) at 390px and 1366px show the sticky year-rail timeline card, the officer ring avatar and member row, and the album tile with its placeholder cover and caption overlay, with no horizontal overflow at 390px. Emulators (8080/9099) reseeded mid-task after the full e2e run's admin soft-delete test mutated `history/h1`, then left running.

Fix round 1: (1) Committee ring photos CSS: `.people .ring img` (line 344) scoped the rule to the home page only; committee.html officer/member cards with `photoUrl` set rendered unclipped raw `<img>` elements. Replaced with a generic `.ring img{width:100%;height:100%;border-radius:50%;object-fit:cover;display:block}` in the bridge block, covering every page where ring images appear. (2) `pageHeader()` listener leak: the function was called on every render (pages re-render on `langchange`), and `onResize(() => paintHeader(c))` registered a new listener each time without unsubscribing the previous one. Added module-level `let unsubHeader = null;` and replaced the bare `onResize()` call with `unsubHeader?.(); unsubHeader = onResize(...)` (pattern borrowed from `home.js`'s `unsubHero`). Verification: `node --check js/shell.js` clean; `npm run seed` complete; `npx playwright test tests/e2e/public.spec.js tests/e2e/theme.spec.js` 8/8 pass. Commit: `css/site.css`, `js/shell.js`, `docs/build-log.md`.

## 2026-09-11 — Phase 5 Task 9 — events + donate
Rewrote `js/pages/events.js` (day `.tabs`, `.timeline` rows via `isLiveEvent`, past `.acc` accordion) and `js/pages/donate.js` (`.upibig` UPI card, `.purpose`/`.pcard`/`.chips` from `parsePurposes(s.donatePurposes)` feeding the hoisted `amountField`, unchanged WhatsApp/copy/`upi://pay` logic, `.wall` donor rows). Found and fixed a real bug while chasing the admin e2e RED: `listPublished` orders events by the admin's free-form `order` field (`Date.now()` for a freshly created event), not by date, so the default day tab picked whichever event was created/ordered first rather than the chronologically nearest one — `up` is now sorted by `start` before deriving `days`/`selectedDay`. Seed gained `design: 'siddhi'`, `donatePurposes`, `sectionVisibility.culture`, and a corrected `address` (was মালদা/Malda, trust is actually in বালুরঘাট/Balurghat, দক্ষিণ দিনাজপুর/Dakshin Dinajpur). No CSS bridge needed — all classes already existed in `css/site.css`.
TDD: `public.spec.js` (`.ph h1`, `.ev` count), `admin.spec.js` (`.ev b`), `donate.spec.js` (`.purpose .pcard` count 2 + first chip `₹৫০১`) edited first — RED confirmed (3 failing) against the old markup via `git stash`; GREEN after the rewrite (`tests/e2e/donate.spec.js`/`public.spec.js`/`theme.spec.js` 10/10, `--project=admin` 25/25 after the day-sort fix, `npm run test:unit` 67/67). Screenshots at 390px/1366px confirm the day tab, event row, past `<details>` (verified open via a temporary emulator-only event, since seeded data has none), and the UPI card + 2 purpose cards + WhatsApp form + wall, with zero horizontal overflow at 390px. Emulators (8080/9099) reseeded after the admin run's soft-delete mutation, then left running.

Fix round 1: (1) `.pulse` styling was scoped to `.ticker .pulse` only (line 307 of `css/site.css`, a bridge rule from Task 3), so the live badges rendered unstyled in `.ev.live` rows (events) and home schedule rows. Changed the selector from `.ticker .pulse` to `.pulse`, keeping all declarations. (2) `js/pages/events.js`: removed dead `num` variable (line 21, never used) and unused `bnDigits` import. (3) `js/pages/donate.js`: rewrote the hoisting comment (lines 27–31) from a 5-line explanation of why the inputs are held at block scope to a simpler single-line statement matching the brief's recommended wording. Verification: `node --check js/pages/events.js js/pages/donate.js` passed; `npm run seed` completed; `npx playwright test tests/e2e/public.spec.js tests/e2e/live.spec.js tests/e2e/donate.spec.js` 9/9 green; external Playwright script (scratchpad) confirmed computed `background-color: rgb(226, 64, 28)` (sindoor) and `animation-name: pulse` on an injected `.pulse` span inside `.ev`. Emulators (8080/9099) left running.

## 2026-09-11 — Phase 5 Task 10 — transparency + members
Rewrote `js/pages/transparency.js`'s render body (kept tab logic, `?preview=1&year=`, `dataFor`, `ledgerTable`, error/empty states exactly): `summary sum` strip, `barsView`/`donutView` (centre = top expense category's % of total), the two `ledgerTable` cards, a `.acc` documents accordion (first entry open), and a `.legal` block (reg. no./80G/address from settings), all now framed by `pageHeader`+`section`. Restyled `js/pages/members.js` only — logged-out form wrapped in `pageHeader`+`section(.mem > .otp.card)`, logged-in dashboard (and the not-a-member state) wrapped in `pageHeader`+`section(.dash > …)` — every existing node, class, id and the OTP/Recaptcha/loadData logic unchanged.
CSS bridge (append-only, two additions, both load-bearing not decorative): (1) `.card` had no base rule anywhere except the scoped `.tl .card` — bare `.card` blocks (transparency's income/expense table cards, members' own-info card) rendered with zero background/border/padding; added a generic `.card` rule matching the pattern already used by `.tile`/`.ccard`/`.otp`/`.stat`/`.notice`. (2) `table.ledger` (the pre-existing breakdown table) and `.ledger` (this task's bars+donut grid wrapper) share a class name — `.ledger`'s `display:grid` was winning on the table (nothing in `table.ledger` declared `display`), turning each `<tr>` into a grid item and laying rows out two-per-row instead of one per row; added `table.ledger{display:table}`, which already outranks `.ledger` on specificity.
TDD: `tests/e2e/transparency.spec.js` edited first (`.acc details summary`/`a` + `.legal` assertions) — RED confirmed against the old markup, then GREEN after the rewrite. Full suite: `npx playwright test` 25/25 green (public+members+admin), `npm run test:unit` 67/67. Screenshots (Playwright, fresh browser, outside repo) at 390px/1366px for both pages confirm the summary strip, bars, donut with correct % + category label, both ledger tables, the accordion, and the legal block, plus the members OTP card and full logged-in dashboard — no horizontal overflow at 390px anywhere. Emulators (8080/9099) reseeded, then left running.

Fix round 1: on desktop, the logged-out members page renders `.mem` (grid-template-columns: 1fr 1fr) with a single `.otp.card` child, leaving an empty right column. Added one bridge rule to `css/site.css`: `.mem > .otp:only-child{grid-column:1 / -1;max-width:560px;width:100%;justify-self:center}` with comment "logged-out members page has only the OTP card — centre it instead of leaving an empty column". Verification: `lsof -i :8080` confirmed emulators (8080/9099) already running; `npm run seed` seeded demo-trust; `npx playwright test --project=members` 22/22 green. Browser preview at 1366px shows the OTP card centered horizontally; at 390px no horizontal overflow. Commit: `css/site.css` + `docs/build-log.md` as `fix(members): centre the lone OTP card on desktop`.

## 2026-09-11 — Phase 5 Task 11 — admin fields
Added the admin-side inputs public pages already consumed: `committee.js` gained an `officer` `boolField` (read into `data.officer`), `albums.js` gained a `featured` `boolField` (read into `data.featured`), and `settings.js` gained `'culture'` in `SECTIONS` (drives both the checkbox and the `sectionVisibility` write) plus a `donatePurposes` textarea field. `forms.js`'s `textField` gained a `{ multiline = false }` option — renders a `<textarea rows=4>` and sets `.value` after creation, since `el()`'s generic `setAttribute` path doesn't set textarea content — existing input call sites unchanged. Seed gained `officer`/`featured` flags: `committee/c1` and `albums/a1` true, `c2`/`a2` false.
TDD: `tests/e2e/public.spec.js`'s "drafts and hidden rows" test got one added line (`.best a` count 1) — RED confirmed against the pre-flag seed (0 received), GREEN after the seed + form changes. Full suite: `npx playwright test --project=public` 15/15, `--project=admin` 25/25 (re-seeded after, since its soft-delete test mutates seed data), `npm run test:unit` 67/67. External Playwright smoke script (scratchpad, outside the repo) logged into `/admin/` and confirmed `input[name=officer]` checked on `#committee/c1`, `input[name=featured]` checked on `#albums/a1`, `textarea[name=donatePurposes]` containing `প্রতিমা`, and `input[name="vis.culture"]` present on `#settings`. Emulators (8080/9099) left running.

## 2026-09-11 — Phase 5 Task 12 — admin design card
Added `admin/js/sections/design.js`: a 🎨 ডিজাইন card listing the five `THEMES` as `.theme-tile` cards (four-colour `.swatch` reading each theme's `--bg`/`--sindoor`/`--pitambar`/`--ivory` via `css/themes.css`'s `[data-theme]` blocks, now loaded in `admin/index.html`), each with a Preview link (`?theme=<name>`, own tab only) and an Apply button gated by `ctx.reauth()` that writes `settings/site.design` and logs an audit row. `css/admin.css` gained the theme-grid/swatch rules plus a `.swatch[data-theme="siddhi"]` block copying site.css's `:root` tokens, since siddhi (the default) has no `[data-theme]` block of its own in themes.css and admin never loads site.css. `admin.js` imports the new section right after settings so its tile lands second on the dashboard (13 tiles total). Named the section object `def` and re-invoke `def.render(box, ctx)` after a successful apply (rather than relying on `this` inside the nested click-handler arrow function) so re-rendering the tile list after a write is unambiguous.
TDD: bumped the three dashboard-tile-count assertions (`tests/e2e/admin.spec.js`, `tests/e2e/live.spec.js`, `tests/e2e/transparency.spec.js`) from 12 to 13 and appended the brief's apply-flow test to `tests/e2e/theme.spec.js` first — confirmed RED (`npx playwright test tests/e2e/theme.spec.js` failed on tile count, 3/4 passing) before writing `design.js`, then GREEN (4/4). Full suite: `npm run e2e` 26/26 (all projects: public incl. theme, members, admin); `npm run test:unit` 67/67; `npm run test:rules` 21/21 (dev emulator stopped first so `emulators:exec` could bind 8080/9099, then restarted and reseeded afterward — admin's soft-delete test also required a reseed after the e2e run). Screenshots (external Playwright script, scratchpad, outside the repo) at 390px and 1366px of `/admin/#design` (logged in) show five tiles with correct four-colour swatches, siddhi outlined as current, and Preview/Apply(current) buttons, with no horizontal overflow at 390px. Emulators (8080/9099) left running, reseeded.

## 2026-09-11 — Phase 5 shell fix: brand/footer line breaks
`scripts/shots.mjs` (Task 13) rendered every page × theme × width and its overflow check was clean (0 warnings), but reading the actual PNGs (`siddhi/index-390.png`, `mukha/donate-1366.png`, `dhokra/transparency-768.png`) showed a real layout defect on every page/theme: the nav brand's trust-name+tagline and the footer's address+"Reg. no." line each ran together on one crowded line with no gap (e.g. "গণেশ পুজো ট্রাস্টসবার পুজো", "দিনাজপুরReg. no. WB/2026/DEMO"). The concept source (`docs/design/concept/full-site-concept.html` lines 315 and 660) puts a `<br>` between each pair; Task 5's `js/shell.js` rewrite dropped both `<br>`s when porting the markup, so the two spans/nodes stayed inline instead of stacking. Fixed both spots in `js/shell.js` (brand: `<br>` inserted between the `.t` and `.s` spans when a tagline exists; footer: `<br>` inserted before the "Reg. no." span when `regNo` exists) — text content unchanged, no test assertions needed updating. Verification: `node scripts/shots.mjs` re-run, still 0 overflow warnings, and the two spots visually confirmed fixed in the regenerated PNGs; `npm run e2e` 26/26 green (dev emulator, reseeded).

## 2026-09-11 — Phase 5 Task 13 — shots, docs, rules deploy, live verification
`scripts/shots.mjs` (120 PNGs: 8 pages × 5 themes × 3 widths) ran clean — 0 `OVERFLOW` warnings — after the shell.js brand/footer fix above; `docs/user-guide/admin-guide.md` (13-row dashboard table in import order, new "ডিজাইন বদলানো" section with the five theme names/descriptions from `THEME_META`, and a "নতুন কিছু ফিল্ড (Phase 5)" section for committee `officer`, album `featured`, settings `donatePurposes`/`vis.culture`), `docs/PROJECT_CONTEXT.md` §6, `docs/pending.md` (Phase 5 shipped + owner steps + deferred minors from the phase's reviews) and `README.md` (themes + `node scripts/shots.mjs`) all updated. Full local gate: `npm run seed && npm run e2e` → **26/26 green**; dev emulator (8080/9099) then stopped, `npm test` → **67 unit + 21 rules, both green**. `scripts/deploy-rules.sh` re-ran the full suite internally (green) then `npx firebase deploy --only firestore:rules,firestore:indexes,storage --project ganesh-puja-trust` — **deploy complete**, tail: "✔ firestore: deployed indexes … successfully", "✔ storage: released rules storage.rules to firebase.storage", "✔ firestore: released rules firestore.rules to cloud.firestore", "✔ Deploy complete!" (2026-09-11 03:46).
Pushed to `origin main` (`728d346..481d228`, two commits: the shell fix above, then this docs+deploy commit). GitHub Pages rebuilt within ~1 min (`curl -sI https://hrishi91.github.io/trust_webpage/css/themes.css` went 404→200). Live verification (Playwright, scratch script outside the repo, against real production data — not the emulator seed): `index.html` → `html[data-theme]` is `siddhi`, `.hero svg.ganesh` visible, **zero console errors**; `index.html?theme=dhokra` → `data-theme` is `dhokra`; `about/committee/gallery/events/donate/transparency/members.html` at 390px → **0px horizontal overflow on every one**. Full-page screenshots of the live home at 390px and 1366px were read and inspected: real committee officer photos, a live 3-event schedule, a real income/expense donut, and the brand/footer fix from the shell entry above both confirmed correctly split onto their own line on production. No code defects found — no follow-up commit needed.

## 2026-09-11 — Phase 5 final review fix wave
Senior whole-branch review of the merged Phase 5 branch found 14 items (7 visible-on-production, 7 minors ruled "fix before done"); base for the fix diff `e52be44`. Working through them as separate commits below, all under this one heading per the project's one-heading-per-subject rule.
Items 1/2/3/7/9 (css/site.css, css/themes.css, tests/unit/contrast.test.js): (1) `.garland` had `height:26px` but no `width` in the concept, so the painted canvas stayed at its default replaced-element size (300×150, clipped to 52×26 rendered) instead of spanning the hero — bridge-appended `.garland{width:100%;display:block}` (concept lacked both properties). (2) mukha sets `--pitambar` equal to its own `--bg` (`#e9b322`), making `.crumb`/`.hero .eyebrow`/`.hero h1 em`/`.countdown b` invisible in that theme; added a new `--hero-accent` token (siddhi `#ffcc66`, mukha `#161311`, dhokra `#f1d27a`, atreyee `#1c6e7a`, bangarh `#f3d58a`) to `:root` and all four `[data-theme]` blocks, and bridge-appended `.crumb,.hero .eyebrow,.hero h1 em,.countdown b{color:var(--hero-accent)}` — re-declaring `color` here is intentional (the concept's `--pitambar` value is the defect, not this rule); new test `hero-accent/bg ≥ 4.5`, all five pass (siddhi 12.25, mukha 9.63, dhokra 12.01, atreyee 5.26, bangarh 4.73). (3) accent-on-ground contrast was untested; added `sindoor/ivory ≥4.5`, `sindoor/card ≥4.5`, `durva/card ≥3.0` (26px bold figures = large text), `ticker-ink/sindoor ≥4.5` to contrast.test.js, replaced `.ticker`'s hardcoded `color:#fff6e8` with `color:var(--ticker-ink)` (new token, `:root` + all four theme blocks — added explicitly everywhere since the test parses each theme block independently, not merged with `:root`), then adjusted tokens minimally until green:
  - siddhi `--sindoor`/`--cta` (kept in sync, siddhi sets `--cta` equal to `--sindoor`) `#e2401c → #c9361a`: sindoor/ivory 3.96→4.90, sindoor/card 4.21→5.22, cta-ink/cta 3.93→4.87, ticker-ink/sindoor 3.93→4.87.
  - dhokra `--sindoor` `#c9502a → #e0663a`: sindoor/ivory 3.82→5.02, sindoor/card 3.46→4.55; the default `--ticker-ink` (`#fff6e8`) on the new sindoor is only 3.20 (fails) — dhokra gets its own `--ticker-ink:#1e1710` (dark text on the orange strip): ticker-ink/sindoor 3.20→5.17.
  - atreyee `--durva` `#7ba23f → #5f8a2e`: durva/card 2.97→4.07.
  - mukha/atreyee/bangarh `--sindoor` and mukha/dhokra/bangarh `--durva` were already passing (mukha sindoor/ivory 5.49, sindoor/card 6.16, durva/card 4.82; atreyee sindoor/ivory 5.06, sindoor/card 5.56; bangarh sindoor/ivory 5.36, sindoor/card 5.76, durva/card 5.12; dhokra durva/card 3.24; siddhi durva/card 4.04→ unaffected by the sindoor change) — left unchanged.
  Also merged `.ph h1`/`.ph p` (each declared twice — the later `.ph p{color:var(--hero-muted)}` never touched `opacity`, so the earlier rule's `opacity:.9` silently survived on every inner-page heading) into one rule each with explicit `opacity:1`, and merged the duplicate `.otp input.code` (line ~196 vs ~283) into the one at ~283, keeping the effective cascade values (`font-size:24px;letter-spacing:.35em;text-align:center;font-weight:700`). (7) 44px touch targets: bridge-appended `.links.open a{min-height:44px;display:flex;align-items:center}` (concept's `.links a` sets neither) and `footer a{min-height:44px;padding-block:.7rem}` (concept's `footer a` already sets `display:block`, so `padding-block` reaches 44px instead of re-declaring `display`, per the brief's fallback). Verification: `node --test 'tests/unit/**/*.test.js'` 69/69 green (67→69, two new contrast tests).
Item 4 (tests/e2e/public.spec.js, scripts/shots.mjs): the mobile-overflow e2e test only checked 5 of the 8 public pages — added `donate`/`transparency`/`members` to the loop. `scripts/shots.mjs` had no failure handling at all — a thrown error on any one page/theme/width combination would kill the whole 120-shot run and leave `process.exitCode` at 0 even when an `OVERFLOW` line was printed; wrapped each page render in try/catch (logs `FAIL theme/page@width: message`, continues), set `process.exitCode = 1` on either an `OVERFLOW` or a `FAIL`, and wrapped the whole matrix in try/finally so `browser.close()` always runs.
Item 5 (tests/e2e/theme.spec.js): the first test switched to `?theme=mukha` and checked `data-theme` but never checked `localStorage`, so a regression that persisted a preview would have passed silently — added `expect(await page.evaluate(() => localStorage.getItem('design'))).toBe('siddhi')` right after the `?theme=mukha` navigation, before the existing about.html persistence check.
Item 6 (tests/e2e/theme.spec.js): the apply-flow test asserted the toast and the `.theme-tile.current` swap but never checked that `logAudit()` (admin/js/sections/design.js line 27: `logAudit(ctx, 'update', 'settings/site', {design: cur.design ?? null}, {design: name})`) actually wrote a Firestore row — added a `GET .../documents/audit` REST read against the emulator (same `Authorization: Bearer owner` pattern as admin.spec.js's soft-delete test) asserting some document has `fields.path.stringValue === 'settings/site'` and `fields.after.mapValue.fields.design.stringValue === 'atreyee'`.
Item 8 (docs/build-log.md): the "one heading per task" rule was broken three times by nested `### Fix round 1` sub-headings (Task 8/9/10 areas) — converted all three to inline `Fix round 1: ...` lines (matching the pattern already used elsewhere in this file), keeping every fact, dropping the sub-heading markup.
Item 10 (js/theme.js, tests/unit/theme.test.js): `js/art.js`'s `onResize()` already listens for a `document` `'themechange'` event (used by `pageHeader()`/`home.js`'s hero to repaint on theme swap), but `applyTheme()` never actually dispatched one — added `document.dispatchEvent(new CustomEvent('themechange'))` at the end of `applyTheme`, dispatched unconditionally (both persisted and preview swaps should repaint theme-coloured canvases). Added `applyTheme`'s first unit test (it had none) using a minimal fake `document`/`localStorage` (theme.js guards on `typeof document === 'undefined'`, so a real `node --test` run was previously a no-op branch): asserts `dataset.theme` is set for a known theme, an unknown name (`'evil'`) falls back to `siddhi`, `persist:false` does not write `localStorage`, and `dispatchEvent` fires in both the persisted and non-persisted case. `node --test 'tests/unit/**/*.test.js'` 70/70 green (69→70).
Item 11 (js/pages/about.js, js/pages/members.js): `pageHeader()`'s `crumb` was set to the same translation key as `title` on every inner page except home, so the breadcrumb line literally repeated the heading below it instead of orienting the reader ("you are on the trust's site, on this page") — `about.js`'s header now reads `crumb: pick(s.name)` (title unchanged, `t('nav.about')`); `members.js`'s three `pageHeader()` call sites (logged-out phone form, not-a-member dashboard, member dashboard) all now read `crumb: pick(s.name)` (titles unchanged).
Item 12 (js/pages/transparency.js): the documents-accordion link text carried its own `↓` (`'ডাউনলোড ↓'`/`'Download ↓'`) on top of the `.acc summary::after{content:"↓"}` CSS glyph already on the parent `<summary>`, showing two down-arrows per closed document row — removed the glyph from the link text (`'ডাউনলোড'`/`'Download'`); `tests/e2e/transparency.spec.js`'s `hasText: 'ডাউনলোড'` locator still matches (substring match).
Item 13 (docs/pending.md): deleted the stale deferred-minor line claiming `pageHeader()` discards `onResize`'s unsubscribe — `js/shell.js` already guards this with a module-level `unsubHeader` (`unsubHeader?.(); unsubHeader = onResize(...)`, fixed in Task 8's own fix round, see the `## 2026-09-11 — Phase 5 Task 8` entry above).
Item 14 (docs/user-guide/admin-guide.md lines 18, 36): the announcements section said the live strip shows "হোমপেজের ওপরে" (at the top of the homepage) — since Task 5's shell rewrite it's a site-wide ticker on every public page. Reworded both lines to "সাইটের প্রতিটা পাতার ওপরে" (at the top of every page on the site), the second line also adding "(শুধু হোমপেজে না)" (not just the homepage) for clarity.

## 2026-09-11 — Phase 5b — admin panel restyled in the design system
The `:root{…}` default (siddhi) token block moved out of `css/site.css` (was lines 1–12) into a new `css/tokens.css` (same 12 lines, prefixed with a comment explaining it is shared by the public site and `/admin/`); `css/site.css` now starts at its `/* ================= base ================= */` comment (`grep -c '^:root' css/site.css` → 0). All 8 public pages (`about/committee/donate/events/gallery/index/members/transparency.html`) gained `<link rel="stylesheet" href="css/tokens.css">` immediately before their existing `css/site.css` link. `tests/unit/contrast.test.js` now reads `css/tokens.css` for the siddhi block instead of parsing `:root` out of `css/site.css`.
`admin/index.html`'s head was rebuilt in token-load order: the inline `localStorage('design')` cache-and-stamp script (same pattern as the public shell), font preconnects + the shared Google Fonts link (Baloo Da 2/Hind Siliguri/Tiro Bangla/Atma), then `../css/tokens.css`, `../css/themes.css`, `../css/admin.css`. `admin/js/admin.js` imports `resolveTheme`/`applyTheme` from `js/theme.js` and, right after the `$` helper, reads `settings/site` (publicly readable, so this resolves before login) and calls `applyTheme(resolveTheme(...))`; `admin/js/sections/design.js` calls `applyTheme(name)` right after its existing `setDoc`/`logAudit` on a successful theme change, so an admin sees their own Apply take effect immediately without a reload. `css/admin.css` was rewritten from scratch against the shared tokens — every selector already in use kept (`.adm-top .grow .card .grid .tile .icon .bi .btn .btn-sm .secondary .danger .row .list-item .badge .pub .err .back .toast .toast-err .thumb .muted .rows-list .rows-item .file-link .theme-grid .theme-tile .current .swatch .s1–.s4 .apply`, `label`, inputs, `progress`, and the `#adm-title/#adm-login/#adm-main` ids), nothing new added: dark indigo `.adm-top` with a pitambar bottom rule, ivory ground, white `.card`s with hairline `--line` borders and `--r` radius, pill `.btn`s in `--cta`, `.grid .tile` bento tiles (min-height 112px, hover/focus lift), 44px input/button targets, and focus rings on `var(--focus)`. The `.swatch[data-theme="siddhi"]` block (needed because the theme card lists all five themes regardless of the page's own active theme) now copies its four literal colours from `css/tokens.css` instead of the old file's stale/wrong values, with a comment explaining why it can't just read the custom properties.
TDD: `tests/unit/contrast.test.js` FAILED first with `ENOENT css/tokens.css` (before the file existed), PASSED after the tokens move (4/4). A new e2e test in `tests/e2e/admin.spec.js` ("admin panel follows the stored theme and uses the site fonts": `data-theme=siddhi`, `.adm-top` background `rgb(15, 18, 48)`, every `.grid .tile` ≥44px tall, then an Apply→atreyee→Apply→siddhi round trip) FAILED first (`data-theme` attribute missing/empty against the old admin.css) before the head/JS/CSS changes, PASSED after (27/27, `--project=admin`). Full gate: `npx playwright test --project=admin` 27/27, reseeded, `--project=public` 16/16, reseeded, `npm run e2e` 27/27 (all three projects); `npm run test:unit` 70/70; dev emulator stopped, `npm test` (70 unit + 21 rules) both green. `node scripts/shots.mjs` (public pages, unaffected by the admin-only CSS but re-run as a regression check on the tokens move) → 0 overflow warnings, exit 0. A scratch Playwright script (outside the repo) shot `/admin/` login, dashboard, `#settings`, `#design`, `#albums`, `#transparency` at 390px/1366px for siddhi and dhokra (theme switched via the 🎨 card, restored after) — read visually: siddhi renders the same dark-indigo-header/ivory-card/pill-button look as the public site; dhokra's dark inputs (`--ivory2` background, `--ink` text) stay readable, the login card pre-paints dark before Firestore answers (the inline cache script), and the siddhi swatch inside the dhokra-themed `#design` card still shows the correct bright siddhi colours; no horizontal overflow on any shot.
CSS fix: the generic `input, textarea, select { min-height:44px; … }` rule was also matching `input[type=checkbox]` and `input[type=file]`, rendering checkboxes as full-width 44px boxes and file inputs with excess padding. Added `input[type=checkbox]{width:22px;height:22px;min-height:0;padding:0;margin:0;flex:0 0 auto;accent-color:var(--sindoor);border-radius:6px}`, `input[type=file]{width:100%;min-height:0;padding:.4rem 0;border:0;background:transparent}`, and `label.row{display:flex;align-items:center;gap:.6rem;min-height:44px;cursor:pointer}` + `label.row span{display:inline;font-size:15px;color:var(--ink);font-weight:500;margin-bottom:0}` to keep checkbox/label pairs on one line. Verification: scratch Playwright script at 390px and 1366px — checkboxes small squares beside labels, no horizontal overflow; `npx playwright test --project=admin` 27/27 green, reseeded.

## 2026-09-11 — Phase 6 spec + plan ("nothing static")
Owner: "nothing static — user can change; UI as template". docs/superpowers/specs/2026-09-11-phase-6-template-complete.md (overrides layered over code defaults: content/strings, content/media slot registry, culture collection, settings.designOverrides/fonts/homeSections/social/estYear/credItems/metaDescription; rules validate colours/fonts/keys) and docs/superpowers/plans/2026-09-11-phase-6-template-complete.md (8 tasks: i18n overrides → rules/content/seed → public rendering → ✏️ লেখা → 🖼️ UI ছবি + 🏺 সংস্কৃতি → 🎨/⚙️ extensions → prod seed/docs/deploy → final review).

## 2026-09-11 — Phase 6 Task 1 — i18n overrides + keyed UI literals
`js/i18n.js` gained the overrides layer required by the spec: `setOverrides/getOverrides` (drops non-object/non-string entries), `defaultString(key)` (untouched default, ignoring overrides), and `t(key, lang)` = non-empty override ?? default. `STRING_GROUPS` is computed from each key's dot-prefix (not hand-maintained) so a new key can never go unclassified or double-classified; a unit test asserts it covers exactly `Object.keys(STRINGS)`.
Every inline `pick({ bn:…, en:… })` literal and admin `{ bn:'…', en:'…' }` label object across `js/pages/{home,donate,transparency,committee,gallery}.js`, `js/shell.js`, `admin/js/admin.js`, and all 13 `admin/js/sections/*.js` moved into `STRINGS` (119 new keys, 91→210 total) and is now read via `t(key)` or `STRINGS[key]`; identical existing text reused where it already matched (e.g. admin section titles for donations/transparency/members/notices/roster/announcements/design/committee/events/gallery/history reuse the existing `admin.*`/`nav.*` key instead of duplicating).
Data fields (`THEME_META`, `CULTURE`, photo `caption` defaults) were left untouched — not UI copy.
TDD: 4 new tests in `tests/unit/i18n.test.js` (override precedence/fallback, malformed-override rejection, group coverage, `defaultString`) FAILED first (missing exports), PASSED after implementation. `npm run test:unit` 74/74 (70→74). `npm run e2e` 27/27 unchanged (all default text preserved byte-for-byte), reseeded after.

## 2026-09-11 — Phase 6 Task 2 — rules, content reads, seed
`firestore.rules`: `content/{doc}` (read: anyone; create/update: `isAdmin() && doc in ['strings','media']`; delete: false) — a fixed two-doc registry, not an open collection, per spec §4. `culture/{id}` gets the same `published`/`deleted` shape as `history`/`events` (`isAdmin() || isLive()` for read, `isAdmin() && hasDeletedFlag()` for write) rather than committee's `isPublic` gate, since culture cards have no separate visibility flag in the data model. `settings/site` write now also requires `validOverrides() && validFonts() && validHomeSections()`: `hexColor(v)` (`^#[0-9a-fA-F]{6}$`) backs `validOverrideValues(o)`, which checks all 15 whitelisted `designOverrides` keys individually (`(!('bg' in o) || hexColor(o.bg)) && …` — rules cannot iterate a map's values, so the brief's `.values().toSet()` placeholder was dropped and each key written out); `validFontValues(f)` restricts `fonts.{display,body}` to the four loaded families or `''` (meaning "theme default" — the admin UI saves `''` for an unset select, so rules must accept it or every untouched save would fail); `validHomeSections()` only checks `is list && size() <= 11` — per-element `{key,on}` shape validation is not expressible in rules and is deferred to `js/sections.js` client-side (Task 3), documented inline.
`js/content.js`: `getContent()` reads `content/strings` + `content/media` independently via `Promise.all`, each with its own `.catch` → `{}` (`console.warn`, never fails closed — unlike `getSettings()`'s maintenance-mode fallback, a broken overrides read just means "no overrides today"), memoised in a module-level promise. `listCulture()` = `listPublished('culture')`, reusing the existing published+deleted+order helper. `DEFAULT_SETTINGS` gained `designOverrides:{}`, `fonts:{display:'',body:''}`, `homeSections:[]`, `social:{facebook:'',youtube:'',instagram:'',whatsappGroup:''}`, `estYear:''`, `credItems:''`, `metaDescription:{bn:'',en:''}` — all defaults reproduce today's site exactly.
`firestore.indexes.json`: added the `culture` composite (`published ASC, deleted ASC, order ASC`), same shape as `history`'s.
`tests/seed/seed.js`: added `culture` to the `recursiveDelete` reset list; seeded `content/strings`/`content/media` as empty maps (defaults apply until an admin edits something); `culture/cu1..cu3` written from `js/culture.js`'s `CULTURE` array (`title`, `tag`, `text`, `imageUrl:''`, `order: i+1`, `published:true`) plus a `cu4` draft (`published:false`) to exercise the read gate; `settings/site` seed gained the same seven Phase 6 fields as `DEFAULT_SETTINGS` (`estYear:2021` as a concrete seeded value, the rest matching the defaults).
TDD: 5 new tests appended to `tests/rules/firestore.test.js` (content docs read/write/doc-id-whitelist/no-delete; culture read gate + `hasDeletedFlag`; `designOverrides` per-key hex validation; `fonts` whitelist-or-empty; `homeSections` list/size cap) FAILED first (21 pass, 5 fail — rules not yet written) then PASSED after the rules changes above (26/26). `npm run test:unit` unaffected, 74/74 green throughout. Verified live: dev emulator started, `npm run seed` ran clean against the new seed script (culture rows and content docs confirmed via the emulator REST API — `culture/cu1` readable anonymously, `culture/cu4` correctly 403s for an anonymous read since it's a draft), `npx playwright test` (all three projects) **27/27 green**, no regressions from the new rules/fields. Dev emulator stopped; final gate `npm test` → **74 unit + 26 rules, both green**.

Fix round 1 (coverage gaps): `tests/rules/firestore.test.js` gained unverified-admin (email_verified: false) tests for content/culture (rules do not change). Added: `E.unverified` rejects `content/strings` write; `E.unverified` rejects `culture/c9` write and `culture/cu-draft` read; `E.admin` rejects `fonts: 'nope'` (string, not map) and `fonts: 5` (number); `homeSections` boundary — exactly 11 items passes, 12 items fails. `npm run test:rules` → **26/26 green** (all 21 existing tests pass unchanged, 5 new test assertions verify the additions).

## 2026-09-11 — Phase 6 Task 3 — overrides rendered on the public site
`js/media-slots.js` (new): `SLOTS` — the fixed 14-slot registry (`hero`, `garland`, `brandMark`, `favicon`, `ogImage`, `donateBand`, `membersTeaser`, `header.about/committee/gallery/events/donate/transparency/members`), each `{id, label:{bn,en}, where:{bn,en}, folder:'public/ui/...', max:1600}`; `mediaUrl(media, id)` returns the slot's URL only when it is a non-empty `https://` string, else `''` (so callers just do `mediaUrl(media,'hero') || fallbackArt()`). `js/sections.js` (new): `HOME_SECTIONS` (the 11 keys in today's fixed render order); `orderSections(list)` turns `settings.homeSections` into a complete, safe ordering — unknown/duplicate keys dropped, missing keys appended `on:true`, non-array input falls back to the default order all-on; `parseCredItems(text)` turns the `bn | en` per-line free text into `[{bn,en}]`.
`js/theme.js` gains `OVERRIDE_KEYS` (the same 15-key whitelist `firestore.rules`' `validOverrideValues()` enforces — kept in sync by hand since rules can't import JS), `FONTS` (the four loaded families), `applyOverrides(overrides, fonts, {persist=true})` (re-validates every key/value against the whitelist and `^#[0-9a-fA-F]{6}$` before touching `document.documentElement.style`, kebab-cases `ctaInk`/`heroAccent`/`tickerInk`, builds the `--display`/`--body` font-stack strings, persists the raw input to `localStorage('designOverrides')`), and `clearOverrides()`. The inline head-cache `<script>` in all 9 HTML files (8 public + `admin/index.html`) was extended, per the brief's explicit inline-size tradeoff, to re-apply the cached `designOverrides` JSON on next paint — it validates value shape (hex regex) and key shape (`/^[a-zA-Z]+$/`) but NOT the key whitelist (too long for an inline script); harmless even if an unknown key round-trips, since the value reaching `style` is always hex-shaped.
`js/shell.js`: `mountShell` now `Promise.all([getSettings(), getContent()])`, calls `setOverrides(c.strings)` before the first render so every `t()` sees overrides, attaches `s.media = c.media`, and calls `applyOverrides(s.designOverrides, s.fonts, {persist: !isPreview(...)})` right after `applyTheme`. `brandMark()` prefers the `brandMark` media slot over `logoUrl` over the ॐ mark. `pageHeader()` gains an `image` option: when set, a `.ph.photo` with an `<img class="ph-img">` (CSS-faded) replaces the painted-canvas background entirely — the two were never designed to layer. A runtime `<link rel=icon>` is set from the `favicon` slot; `document.title`/`<meta name=description>` pick up `settings.metaDescription` (documented GitHub-Pages limitation: static `<meta>` is what non-JS scrapers see). Footer gains a `.social` row (only non-empty of `facebook/youtube/instagram/whatsappGroup`, brand names left untranslated like the existing `EN`/`বাং` toggle) and the Task 1 leftover `` `Reg. no. ${s.regNo}` `` literal is now `` `${t('tr.regNo')} ${s.regNo}` ``. `admin/js/admin.js`'s theme-follow line becomes `Promise.all([getDoc(settings), getContent()]).then(...)` — also `setOverrides`/`applyOverrides`/`applyStrings()` so the admin panel's own labels and colours follow overrides too; `onAuthStateChanged`'s `route()` may resolve first (labels update on the next route, acceptable per the brief). `admin/js/sections/export.js`'s `'JSON ⬇'` literal moves to a new `admin.exportJson` key in `js/i18n.js` (Task 1 leftover).
`css/site.css` bridge-only additions: `.hero.photo .art img`, `.ph.photo .ph-img` (absolute, cover, opacity .55), `.ccard img` (4:3 cover), `.social`, `.donate .band-img`, `.members .members-img`.
TDD: `tests/unit/media-slots.test.js` (SLOTS shape/uniqueness, `mediaUrl` https-only) and `tests/unit/sections.test.js` (`HOME_SECTIONS`, `orderSections` dedup/fallback/append, `parseCredItems`) are new; `tests/unit/theme.test.js` gained `OVERRIDE_KEYS`/`FONTS` shape and `applyOverrides`/`clearOverrides` tests (fake `document.documentElement.style` + `localStorage`, same pattern as the existing `applyTheme` test). All FAILED first (missing modules/exports), PASSED after (`npm run test:unit` 74→86).
